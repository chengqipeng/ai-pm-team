# 画布左右联动完整设计方案

> 版本：2.0 | 日期：2026-04-23
> 目标：统一所有联动场景的配置模型，覆盖主子、多级级联、多对多关联

---

## 一、问题全景

当前系统有三种数据关系，但联动机制只覆盖了前两种：

| 关系类型 | 数据来源 | 当前支持 | 典型场景 |
|:---------|:---------|:---------|:---------|
| 父子包含（1:N） | p_meta_link | ✅ 已支持 | entity→item, metaModel→metaItem |
| 实例关联（M:N） | entityLink | ⚠️ 部分支持 | role→user, department→user |
| 同模型层级（树） | parent_metadata_api_key | ❌ 不支持 | province→city→district, role→子角色 |

缺失的核心能力：
1. **多级级联**：省→市→区，需要三级联动，当前只支持一级
2. **动态过滤值**：过滤条件的值需要来自上级选中项，当前只支持静态值
3. **多对多中间表**：role→user 通过 p_user_role 关联，需要走专用 API
4. **同模型树形联动**：选中父角色后显示子角色列表

---

## 二、关系模型统一抽象

所有联动场景归纳为 **5 种联动模式**：

### 模式 A：主子联动（父子包含）

```
左侧选中 entity(account) → 右侧加载 item(entityApiKey=account)
左侧选中 metaModel(role) → 右侧加载 metaItem(metamodelApiKey=role)
```

**关系来源**：p_meta_link（referItemApiKey 指定子数据的关联字段）
**过滤方式**：子数据的 `referItemApiKey` 字段 = 父数据的 apiKey
**当前状态**：✅ 已实现（模式二、模式三）

### 模式 B：同模型详情联动

```
左侧选中 role(systemAdmin) → 右侧 detailHeader 显示 systemAdmin 的详情
```

**关系来源**：同一个 metamodelApiKey
**过滤方式**：右侧数据的 apiKey = 左侧选中值
**当前状态**：✅ 已实现（模式一）

### 模式 C：跨模型关联联动（多对多）

```
左侧选中 role(systemAdmin) → 右侧加载该角色下的 user 列表
左侧选中 department(techDept) → 右侧加载该部门下的 user 列表
```

**关系来源**：entityLink（parentEntityApiKey→childEntityApiKey）
**过滤方式**：需要通过中间表或关联字段查询
**当前状态**：⚠️ 部分支持（filterField 机制可用，但配置复杂）

### 模式 D：同模型层级联动（树形级联）

```
左侧选中 province(beijing) → 右侧加载 city(parentMetadataApiKey=beijing)
左侧选中 city(beijingCity) → 右侧加载 district(parentMetadataApiKey=beijingCity)
左侧选中 role(管理层) → 右侧加载 role(parentMetadataApiKey=管理层)
```

**关系来源**：parent_metadata_api_key 字段（同元模型内的层级关系）
**过滤方式**：子数据的 parentMetadataApiKey = 父数据的 apiKey
**当前状态**：❌ 不支持（需要 $动态值 + 多级联动）

### 模式 E：多级级联联动

```
省份列表 → 城市列表 → 区县列表（三级）
实体列表 → 字段列表 → 选项值列表（三级）
```

**关系来源**：模式 A 或 D 的链式组合
**过滤方式**：每级的过滤值来自上一级的选中项
**当前状态**：❌ 不支持（需要 $动态值 + cascadePanel 组件）

---

## 三、统一联动配置模型

### 3.1 FilterCondition 扩展：$动态值

```typescript
interface FilterCondition {
  field: string;
  op: 'eq' | 'ne' | 'gt' | 'lt' | 'gte' | 'lte' | 'like' | 'in';
  value: unknown;  // 静态值 或 '$stateKey' 动态引用
}
```

`value` 以 `$` 开头时，运行时从 `PageContext.state` 中取值：

```json
{ "field": "parentMetadataApiKey", "op": "eq", "value": "$selectedProvince" }
```

运行时解析：`$selectedProvince` → `PageContext.state.selectedProvince` → `"beijing"`

**动态值为空时（上级未选中），该条件自动跳过**，不会过滤掉所有数据。

### 3.2 DataSourceConfig 不变

现有的 `DataSourceConfig` 已经足够表达所有联动场景，不需要新增字段：

```typescript
interface DataSourceConfig {
  type: DataSourceType;
  metamodelApiKey?: string;
  entityApiKey?: string;
  depends?: string[];           // 监听哪些 stateKey 的变化
  stateKey?: string;            // 选中后写入哪个 stateKey
  filters?: FilterCondition[];  // 过滤条件（支持 $动态值）
  filterLogic?: 'and' | 'or';
  filterField?: string;         // 跨模型联动（模式 C）
  filterFrom?: string;
  lazy?: boolean;
}
```

### 3.3 五种模式的配置方式

#### 模式 A：主子联动

```json
// 左侧：实体列表
{ "type": "metadata", "metamodelApiKey": "entity", "stateKey": "selectedEntity" }

// 右侧：字段列表（自动联动，depends 由系统推导）
{ "type": "metadata", "metamodelApiKey": "item", "depends": ["selectedEntity"] }
```

> 系统已有能力，无需改动。

#### 模式 B：同模型详情

```json
// 左侧：角色列表
{ "type": "metadata", "metamodelApiKey": "role", "stateKey": "selectedRole" }

// 右侧：角色详情
{ "type": "metadata", "metamodelApiKey": "role", "depends": ["selectedRole"] }
```

> 系统已有能力，无需改动。

#### 模式 C：跨模型关联（多对多）

```json
// 左侧：角色列表
{ "type": "metadata", "metamodelApiKey": "role", "stateKey": "selectedRole" }

// 右侧：用户列表（通过 filterField 过滤）
{ "type": "entityData", "entityApiKey": "user",
  "depends": ["selectedRole"],
  "filterField": "roleApiKey", "filterFrom": "apiKey" }
```

> 系统已有能力（模式四），但配置复杂。后续可通过 entityLink 自动推导 filterField。

#### 模式 D：同模型层级联动 ← 新增

```json
// 左侧：省份列表
{ "type": "metadata", "metamodelApiKey": "globalPickOption",
  "filters": [{ "field": "entityApiKey", "op": "eq", "value": "province" }],
  "stateKey": "selectedProvince" }

// 右侧：城市列表（parentMetadataApiKey = 选中的省份）
{ "type": "metadata", "metamodelApiKey": "globalPickOption",
  "filters": [
    { "field": "entityApiKey", "op": "eq", "value": "city" },
    { "field": "parentMetadataApiKey", "op": "eq", "value": "$selectedProvince" }
  ],
  "stateKey": "selectedCity" }
```

> 需要 $动态值 支持（已实现）。

#### 模式 E：多级级联 ← 新增

**方案一：cascadePanel 组件（推荐）**

一个组件内完成多级选择，不需要多个 splitPanel：

```json
{ "type": "cascadePanel", "props": {
  "layoutZone": "left",
  "levels": [
    { "label": "省份", "metamodelApiKey": "globalPickOption", "entityApiKey": "province",
      "stateKey": "selectedProvince" },
    { "label": "城市", "metamodelApiKey": "globalPickOption", "entityApiKey": "city",
      "parentField": "parentMetadataApiKey", "stateKey": "selectedCity" },
    { "label": "区县", "metamodelApiKey": "globalPickOption", "entityApiKey": "district",
      "parentField": "parentMetadataApiKey", "stateKey": "selectedDistrict" }
  ]
}}
```

**方案二：多个 listPanel + $动态值（灵活但复杂）**

用 Tab 内放多个列表，每个列表用 `$` 引用上级：

```json
// Tab 1: 城市列表
{ "type": "metadata", "metamodelApiKey": "globalPickOption",
  "filters": [
    { "field": "entityApiKey", "op": "eq", "value": "city" },
    { "field": "parentMetadataApiKey", "op": "eq", "value": "$selectedProvince" }
  ],
  "stateKey": "selectedCity" }

// Tab 2: 区县列表
{ "type": "metadata", "metamodelApiKey": "globalPickOption",
  "filters": [
    { "field": "entityApiKey", "op": "eq", "value": "district" },
    { "field": "parentMetadataApiKey", "op": "eq", "value": "$selectedCity" }
  ]}
```

---

## 四、完整场景矩阵

### 4.1 所有可配置的联动场景

| 场景 | 模式 | 左侧 | 右侧 | 联动字段 | 配置方式 |
|:-----|:-----|:-----|:-----|:---------|:---------|
| 元模型→字段 | A | metaModel | metaItem | metamodelApiKey | depends 自动推导 |
| 元模型→关联 | A | metaModel | metaLink | parentMetamodelApiKey | depends 自动推导 |
| 元模型→选项 | A | metaModel | metaOption | metamodelApiKey | depends 自动推导 |
| 实体→字段 | A | entity | item | entityApiKey | depends 自动推导 |
| 实体→校验规则 | A | entity | checkRule | entityApiKey | depends 自动推导 |
| 实体→业务类型 | A | entity | busiType | entityApiKey | depends 自动推导 |
| 实体→查重规则 | A | entity | duplicateRule | entityApiKey | depends 自动推导 |
| 实体→共享规则 | A | entity | sharingRule | entityApiKey | depends 自动推导 |
| 角色→角色详情 | B | role | role(detail) | apiKey | depends 自动推导 |
| 部门→部门详情 | B | department | department(detail) | apiKey | depends 自动推导 |
| 角色→用户 | C | role | user | roleApiKey | filterField 配置 |
| 部门→用户 | C | department | user | departApiKey | filterField 配置 |
| 省→市 | D | globalPickOption(province) | globalPickOption(city) | parentMetadataApiKey | $selectedProvince |
| 市→区 | D | globalPickOption(city) | globalPickOption(district) | parentMetadataApiKey | $selectedCity |
| 省→市→区 | E | cascadePanel | — | parentMetadataApiKey | levels 配置 |
| 实体→字段→选项 | E | cascadePanel | — | entityApiKey/itemApiKey | levels 配置 |
| 角色→子角色 | D | role | role(children) | parentMetadataApiKey | $selectedRole |
| 部门→子部门 | D | department | department(children) | parentMetadataApiKey | $selectedDept |
| 选项集→依赖 | A | globalPickOption | globalPickDependency | entityApiKey | depends 自动推导 |
| 选项集→子选项 | D | globalPickOption | globalPickOption | parentMetadataApiKey | $selectedPick |

### 4.2 多对多关联的完整路径

| 关联 | 中间表/字段 | API 路径 | 配置方式 |
|:-----|:-----------|:---------|:---------|
| role → user | p_user_role | `/auth/users?roleApiKey=xxx` | filterField=roleApiKey |
| department → user | p_user.depart_api_key | `/entity/data/user?departApiKey=xxx` | filterField=departApiKey |
| account → contact | entityLink(account→contact) | `/entity/data/contact?accountApiKey=xxx` | filterField=accountApiKey |
| account → opportunity | entityLink(account→opportunity) | `/entity/data/opportunity?accountApiKey=xxx` | filterField=accountApiKey |

---

## 五、属性面板配置辅助

### 5.1 联动标识（stateKey）

```
┌─────────────────────────────────────┐
│ 联动标识                              │
│ [selectedProvince          ] [自动]  │
│                                      │
│ 当前: selectedProvince               │
│ · 过滤条件值用 $标识 引用其他组件      │
│                                      │
│ 页面中其他组件的联动标识：              │
│ ┌──────────────────────────────────┐ │
│ │ selectedProvince  selectedCity   │ │
│ └──────────────────────────────────┘ │
└─────────────────────────────────────┘
```

- 输入框可手动填写，placeholder 显示自动推导值
- 底部蓝色标签展示同页面其他组件的联动标识
- "自动"按钮一键恢复自动推导

### 5.2 过滤条件值选择器

```
┌─────────────────────────────────────┐
│ 所属实体    =    ×                   │
│ ┌─────────────────────────────────┐ │
│ │ 选择值...                        │ │
│ │ ── 联动引用 ──                   │ │
│ │ $ selectedProvince (列表面板)    │ │
│ │ $ selectedCity (列表面板)        │ │
│ │ ── 静态值 ──                     │ │
│ │ province                        │ │
│ │ city                            │ │
│ │ district                        │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

- 下拉分两组：联动引用（绿色 `$`）和静态值（从数据库加载）
- `$` 引用的值变化时自动触发数据重新加载

### 5.3 cascadePanel 级联层级配置

```
┌─────────────────────────────────────┐
│ 级联层级 (JSON)                      │
│ ┌─────────────────────────────────┐ │
│ │ [                               │ │
│ │   { "label": "省份",            │ │
│ │     "metamodelApiKey": "...",   │ │
│ │     "entityApiKey": "province", │ │
│ │     "stateKey": "selectedProv"  │ │
│ │   },                            │ │
│ │   { "label": "城市", ... }      │ │
│ │ ]                               │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

> 后续 P1 可改为可视化编辑器（拖拽排序 + 表单配置每级参数）。

---

## 六、实现状态

| 能力 | 状态 | 说明 |
|:-----|:-----|:-----|
| 模式 A 主子联动 | ✅ 已实现 | depends 自动推导 |
| 模式 B 同模型详情 | ✅ 已实现 | metadata loader 按 apiKey 过滤 |
| 模式 C 跨模型关联 | ✅ 已实现 | filterField + filterFrom |
| 模式 D 层级联动 | ✅ 已实现 | $动态值 + stateKey 可编辑 |
| 模式 E 多级级联 | ✅ 已实现 | cascadePanel 组件 |
| $动态值解析 | ✅ 已实现 | applyFilters 支持 $stateKey |
| stateKey 可编辑 | ✅ 已实现 | LinkageKeyEditor 组件 |
| 联动标识辅助 | ✅ 已实现 | collectLinkageKeys 扫描同页面组件 |
| 过滤条件 $引用选择器 | ✅ 已实现 | FilterConditionEditor optgroup |
| cascadePanel 级联面板 | ✅ 已实现 | CascadePanelAtom 组件 |
| cascadePanel 可视化配置 | 📋 P1 | 当前用 JSON 配置 |
| entityLink 自动推导 filterField | 📋 P1 | 当前需手动配置 |
| 级联重置（选省后清空市区） | ✅ 已实现 | cascadePanel 内置 |

---

## 七、配置示例汇总

### 7.1 省→市→区（cascadePanel）

```json
{
  "components": [
    { "type": "splitPanel", "props": { "leftWidth": 360 } },
    { "type": "cascadePanel", "props": {
      "layoutZone": "left",
      "levels": [
        { "label": "省份", "metamodelApiKey": "globalPickOption",
          "entityApiKey": "province", "stateKey": "selectedProvince" },
        { "label": "城市", "metamodelApiKey": "globalPickOption",
          "entityApiKey": "city", "parentField": "parentMetadataApiKey",
          "stateKey": "selectedCity" },
        { "label": "区县", "metamodelApiKey": "globalPickOption",
          "entityApiKey": "district", "parentField": "parentMetadataApiKey",
          "stateKey": "selectedDistrict" }
      ]
    }},
    { "type": "detailHeader", "props": {
      "dataSource": { "type": "metadata", "metamodelApiKey": "globalPickOption",
        "filters": [{ "field": "entityApiKey", "op": "eq", "value": "district" }],
        "depends": ["selectedDistrict"] }
    }}
  ]
}
```

### 7.2 实体→字段→选项值（cascadePanel）

```json
{
  "type": "cascadePanel", "props": {
    "layoutZone": "left",
    "levels": [
      { "label": "实体", "metamodelApiKey": "entity",
        "stateKey": "selectedEntity" },
      { "label": "字段", "metamodelApiKey": "item",
        "parentField": "entityApiKey",
        "stateKey": "selectedItem" },
      { "label": "选项值", "metamodelApiKey": "pickOption",
        "parentField": "itemApiKey" }
    ]
  }
}
```

### 7.3 角色管理（主子 + 多对多）

```json
{
  "components": [
    { "type": "splitPanel", "props": { "leftWidth": 280 } },
    { "type": "listPanel", "props": {
      "layoutZone": "left",
      "dataSource": { "type": "metadata", "metamodelApiKey": "role" }
    }},
    { "type": "detailHeader", "props": {
      "dataSource": { "type": "metadata", "metamodelApiKey": "role",
        "depends": ["selected_role_apiKey"] }
    }},
    { "type": "dynamicTabs", "props": {
      "tabs": [
        { "key": "users", "label": "角色成员",
          "slot": [{ "type": "dataTable", "props": {
            "dataSource": { "type": "entityData", "entityApiKey": "user",
              "depends": ["selected_role_apiKey"],
              "filterField": "roleApiKey", "filterFrom": "apiKey" }
          }}]},
        { "key": "children", "label": "子角色",
          "slot": [{ "type": "dataTable", "props": {
            "dataSource": { "type": "metadata", "metamodelApiKey": "role",
              "filters": [{ "field": "parentMetadataApiKey", "op": "eq",
                "value": "$selected_role_apiKey" }] }
          }}]}
      ]
    }}
  ]
}
```

### 7.4 全局选项集管理（层级 + 子选项）

```json
{
  "components": [
    { "type": "splitPanel", "props": { "leftWidth": 280 } },
    { "type": "listPanel", "props": {
      "layoutZone": "left",
      "dataSource": { "type": "metadata", "metamodelApiKey": "globalPickOption",
        "filters": [{ "field": "entityApiKey", "op": "eq", "value": null }],
        "stateKey": "selectedPickGroup" }
    }},
    { "type": "dataTable", "props": {
      "dataSource": { "type": "metadata", "metamodelApiKey": "globalPickOption",
        "filters": [{ "field": "entityApiKey", "op": "eq",
          "value": "$selectedPickGroup" }] }
    }}
  ]
}
```
