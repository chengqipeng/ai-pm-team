server:
  port: 8001

spring:
  application:
    name: neo-ai-agent-runtime-demo

eureka:
  client:
    service-url:
      defaultZone: https://admin:AdminEureka1234@discovery-dev.ingageapp.com/eureka/
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90

# LLM 模型配置（环境变量优先）
model:
  name: deepseek-v4-flash
  api_key: ${AGENT_API_KEY:sk-HdY98AcN68JhtXLp8oeIATEL4PWq9rzRcCAhI8G4SOtBbtSw}
  api_base: ${AGENT_API_BASE:https://tokenhub.tencentmaas.com/v1}
