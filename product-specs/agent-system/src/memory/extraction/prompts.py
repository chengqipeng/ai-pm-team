"""记忆提取提示词

v3: 统一单次提取（UNIFIED_EXTRACT_PROMPT）— 两阶段推理 + 决策树判定
v2: 四路并行提取（已废弃，保留供回退参考）

v3 设计原则：
1. 两阶段推理（CoT）：先逐句分类归属，再按维度提取内容
2. 决策树判定：用单向 if-else 树代替互相引用的排除规则
3. 1:N 允许：同一句话可归属 1~2 个维度
4. 输出结构兼容：与 v2 下游 schema 完全兼容
5. 已有状态注入：支持增量合并
6. 精细边界规则：补回 v2 经实战验证的 litmus test
"""

# ═══════════════════════════════════════════════════════════
# v3: 统一提取提示词（当前使用）
# ═══════════════════════════════════════════════════════════

UNIFIED_EXTRACT_PROMPT = """你是一个记忆提取引擎。从用户发言中提取可跨会话复用的长期记忆，分为四个维度输出。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 四维度定义

| 维度 | 本质问题 | 典型信号 |
|:---|:---|:---|
| profile | "这个用户/团队是谁" | 身份、职位、公司、团队规模、KPI、工具栈、职场状态 |
| preferences | "这个用户是什么样的人" | 喜欢/讨厌/习惯/在意/偏好 + 个人态度 |
| agent_rules | "Agent 以后应该怎么做" | 约束 Agent 输出格式/语气/长度/流程/角色/禁止项 |
| entities | "第三方是什么情况" | 客户/竞对/联系人的事实、偏好、反馈、关系 |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 决策树（逐句判定归属）

对用户每句有效发言，按以下顺序判定：

```
1. 该信息下个新会话还有价值吗？
   ├─ 否 → skip（单次指令/临时状态/操作请求/疑问句/纯查询）
   └─ 是 ↓

2. 核心主语是谁？
   ├─ 第三方（客户/竞对/联系人/合作方）→ entities
   │    └─ 但如果是 CRM 字段值（金额/电话/邮箱/阶段）→ skip（仅保留实体名）
   ├─ "我们团队/公司/部门" 的集体属性 → profile
   ├─ "我/我个人" ↓
   │    3. 描述的是什么？
   │    ├─ 稳定身份（职位/履历/地域/规模/KPI/职场状态）→ profile
   │    ├─ 个人态度/习惯/标准（态度动词驱动）→ 走 Rule A 细化判定
   │    └─ 对第三方的判断（"我觉得客户X…"）→ entities
   └─ Agent / Agent 产物（报告/回复/邮件/分析/输出）↓
        4. 是持久约束还是单次调整？
        ├─ 持久（以后/每次/总是/所有/默认）→ agent_rules
        └─ 单次（这次/刚才/帮我/现在）→ skip
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 精细边界规则（决策树叶子节点的 litmus test）

### Rule A: preferences vs agent_rules（最高频冲突）

对节点 3 判定为"个人态度/习惯"的句子，追加 litmus test：

> **"去掉这句话，Agent 以后的输出行为会改变吗？"**

| 判定结果 | 归属 |
|:---|:---|
| 不会改变（纯态度表达，如"我喜欢简洁"）| 仅 preferences |
| 会改变（如"回复不要超过500字"）| 仅 agent_rules |
| 既表达个人态度，又实际约束 Agent 行为（如"我喜欢表格格式，以后数据都用表格"）| preferences + agent_rules |

关键区分信号：
- 纯态度词（喜欢/讨厌/在意/偏好）且无行动指令 → preferences
- 含行动指令（用/不要/必须/总是 + Agent 输出物）→ agent_rules
- 态度词 + 行动指令同时出现 → 两者都提取

### Rule B: agent_rules vs entities（动作指向判定）

> **"这句话是'命令 Agent 做某事'还是'告知 Agent 一个事实'？"**

| 句式 | 归属 |
|:---|:---|
| "跟客户X沟通要用正式语气" → 命令 Agent 行为 | agent_rules |
| "客户X那边要求正式沟通" → 陈述第三方要求 | entities |
| "客户X喜欢简洁，跟他沟通别啰嗦" → 事实+命令 | entities + agent_rules |

### Rule C: profile vs entities（关系动词判定）

> **"句中是否有关系动词串联到第三方当前事实？"**

| 句式 | 归属 |
|:---|:---|
| "我之前在腾讯做过3年" → 纯履历 | profile |
| "我之前在腾讯，他们的审批流程很复杂" → 履历+第三方事实 | profile + entities（拆句） |
| "我们跟客户A合作了2年" → 关系动词串联到第三方 | entities |

### Rule D: entities 的增量认知判定

> **"这条信息是 CRM/系统中已有的结构化字段，还是系统查不到的洞察？"**

| 类型 | 处理 |
|:---|:---|
| 结构化字段值（金额/阶段/电话/邮箱/成立日期）| skip，仅保留实体名作定位锚点 |
| 增量认知（决策风格/内部关系/沟通偏好/未公开信息）| 提取到 entities |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 复合句处理

当一句话包含主语切换时，拆分判定：
- 前半句和后半句可能归属不同维度
- 各部分独立走决策树
- 最终同一句话最多归属 2 个维度

示例："我是产品经理（profile），客户B决策很快（entities）"→ 拆为两条

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 各维度提取规则

### profile
- 与已有画像增量合并，输出【完整】版本
- 新旧冲突时以新信息为准
- 含：职位、履历、地域、团队规模、KPI、职场状态（新入职/调岗/在熟悉业务）
- 含：所属组织的集体属性（业务、规模、文化、工具栈、覆盖行业）
- 无新增 → content 为空字符串

### preferences
- 按语义维度分条，每条一个 slug（英文下划线）
- 常见 slug 示例：reply_format, data_display, work_habit, communication_style, quality_standard, tool_preference, learning_style
- 同 slug 仅保留一条（最新状态）
- 一句话涉及多个 slug 时，分别提取为独立条目
- 无偏好 → 空数组

### agent_rules
- 与已有规则增量合并，输出【完整】版本
- 新旧冲突时以新规则为准
- 无新增 → content 为空字符串

### entities
- 按 merge_key 分条（公司名/人名）
- 仅提取增量认知（系统查不到的洞察），不重复已有实体中的内容
- parent_entity 标注层级（联系人→所属公司）
- 无实体 → 空数组

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 已有状态（用于增量合并）

### 已有画像
{existing_profile}

### 已有 Agent 规则
{existing_rules}

### 已有实体索引
{existing_entities}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 用户发言
{user_messages}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 输出格式

先在 _reasoning 中逐句写出归属判定（标注使用了哪条 Rule），然后在 result 中输出提取结果。

```json
{{
  "_reasoning": [
    {{"sentence": "原句关键词摘要", "decision": "profile/preferences/agent_rules/entities/skip", "rule": "决策树节点X / Rule A/B/C/D", "why": "一句话理由"}}
  ],
  "result": {{
    "profile": {{"content": "合并后完整画像"}},
    "preferences": [
      {{"slug": "英文下划线", "abstract": "≤50字摘要", "overview": "Markdown≤200字", "content": "完整叙述"}}
    ],
    "agent_rules": {{"content": "合并后完整规则"}},
    "entities": [
      {{"abstract": "≤50字", "overview": "Markdown≤200字", "content": "完整叙述", "merge_key": "实体名", "parent_entity": "父实体"}}
    ]
  }}
}}
```

## 硬约束
- abstract ≤50 字；overview Markdown ≤200 字；content 完整叙述
- 文本语言：{output_language}（auto 则跟随对话主要语言）
- 无提取内容的维度：profile/agent_rules 的 content 为空字符串，preferences/entities 为空数组
- 只输出 JSON，不加任何其他文字
"""


# ═══════════════════════════════════════════════════════════
# v2: 四路并行提取提示词（已废弃，保留供回退参考和 eval 对比）
# ═══════════════════════════════════════════════════════════

_COMMON_OUTPUT_RULES = """
## 输出规则
- abstract ≤50 字；overview 为 Markdown（`##`+`-`）≤200 字；content 完整叙述
- 文本使用 {output_language}（auto 则跟随对话主要语言）
- 多维度混合：同句含多维度时，各维度独立判断，各自提取属于自己的部分
- 只输出 JSON，不写其他说明
"""


# [DEPRECATED] Profile — 用户画像
PROFILE_EXTRACT_PROMPT = """从用户发言中提取【稳定、长期有效】的身份背景，合并到已有画像后输出完整版本。

## 识别原则
用户对【我 / 我们团队 / 我们公司】的稳定属性描述：
- 职位 / 履历 / 所在区域 / 长期可量化指标（KPI、预算、团队或客户规模）
- 职场状态（新入职 / 在熟悉业务 / 调岗等当前身份阶段）
- 所属组织的业务、规模、文化、工具、覆盖行业
- 允许省略主语；从语义可判为用户自述即可

判别：**该信息在下一个新会话里对 Agent 理解"这个用户是谁"是否仍有帮助**。

## 排除规则
以下内容不属于本路，即使句中出现"我"字：
- 个人喜好、习惯、厌恶（态度动词：喜欢/讨厌/习惯/在意）→ preferences
- 对 Agent 行为的持久指令（动作指向 Agent 产物）→ agent_rules
- 第三方事实（核心主语是第三方，含"客户反馈我们…"）→ entities
- 单次操作、临时状态、孤立数值 → 不提取

## 边界判定
- profile vs preferences：集体属性（"我们部门/团队/公司"的文化、制度、工具）→ profile；个人态度（"我个人喜欢/习惯/在意"）→ preferences
- profile vs entities：主语是"我/我们"描述自身特征 → profile；主语是第三方或描述第三方事实 → entities
- 提取 vs 不提取：稳定身份信息（跨会话仍有效）→ 提取；临时状态（"今天心情不好"）→ 不提取

## 已有画像
{existing_profile}

## 用户发言
{user_messages}

## 输出格式
- 有新增：`{{"profile": {{"content": "<合并后完整画像>"}}}}`
- 无新增：`{{"profile": {{}}}}`
""" + _COMMON_OUTPUT_RULES


# [DEPRECATED] Preferences — 用户自己的喜好/习惯/标准
PREFERENCES_EXTRACT_PROMPT = """从用户发言中提取【用户自己】的喜好、习惯、厌恶或个人标准。

## 识别原则
提取"描述用户本人是什么样的人"的可复用信息：
- 个人喜好与厌恶（喜欢/讨厌/受不了/偏好/倾向）
- 稳定的行为习惯（频率 / 条件 / 时间模式）
- 个人要求的品质标准（在意/要求/觉得应该）

判别：**该信息脱离当前上下文后，在下一个新会话里对理解"这个用户"是否仍有价值**。

## 排除规则
以下内容不属于本路，本路返空：
- 动作指向 Agent 产物的持久指令（主语是报告/回复/邮件/分析等 Agent 输出物，或省略主语但语义上约束 Agent 行为）→ agent_rules。判定标准：如果去掉这句话 Agent 的输出行为会改变，则属于 agent_rules 而非 preferences。
- 描述组织/团队的集体属性 → profile
- 描述第三方的偏好或事实 → entities
- 单次反馈或操作指令（"这次 X"、"帮我 X"）→ 不提取

## 边界判定
- preferences vs agent_rules：核心主语是"我"且表达个人态度（无行为约束意图）→ preferences；句子的效果是约束 Agent 未来输出行为（格式/语气/长度/内容/频率）→ agent_rules。当一句话同时含个人态度和 Agent 行为约束时，仅提取个人态度部分到 preferences，行为约束部分留给 agent_rules 路。
- preferences vs profile："我个人"的态度/习惯/风格 → preferences；"我们部门/团队/公司"的集体属性 → profile
- 提取 vs 不提取：可跨会话复用的稳定偏好 → 提取；单次场景的临时调整 → 不提取

## 粒度
- 按语义维度分条（reply_format、data_display、work_habit、communication 等 slug）
- 同一维度仅保留一条（最新状态）

## 用户发言
{user_messages}

## 输出格式
```json
{{"preferences": [{{"slug": "<英文下划线>", "abstract": "<1 句>", "overview": "<Markdown>", "content": "<完整叙述>"}}]}}
```
无偏好返回 `{{"preferences": []}}`。
""" + _COMMON_OUTPUT_RULES


# [DEPRECATED] Agent Rules — 对 Agent 未来行为的持久约束
AGENT_RULES_EXTRACT_PROMPT = """从用户发言中提取【对 Agent 未来行为的持久约束】，合并到已有规则后输出完整版本。

## 识别原则
两个条件同时满足：
1. **行为指向 Agent**：约束 Agent 的输出、沟通、流程、角色或禁止事项。标志：主语或隐含主语是 Agent / 报告 / 回复 / 邮件 / 分析等 Agent 产物。
2. **持久性**：规则可复用到未来同类场景，不是"这次/刚才"的单次调整。

判别：**该规则在下一次类似请求中 Agent 是否应继续遵守**。

## 排除规则
以下内容不属于本路，本路返空：
- 句子整体描述用户本人的态度/习惯/性格（主语是"我"，谓语是态度动词）→ preferences
- 纯身份或组织描述 → profile
- 对第三方事实的纯陈述（动作未指向 Agent，而是描述第三方的要求/特点/状态）→ entities。判定标准：句子是在"告知 Agent 一个事实"还是"命令 Agent 做某事"？前者是 entities，后者是 agent_rules。
- 单次调整（"这次 X"）、评价（"感觉你像 X"）、感叹 → 不提取

## 边界判定
- agent_rules vs preferences：句中存在泛化的 Agent 行为约束（格式/长度/频率/渠道/流程）→ agent_rules；仅表达个人态度无 Agent 行为约束 → preferences。一句话同时含两者时各路独立提取。
- agent_rules vs entities：动作指向 Agent（"跟 <实体> 沟通要 X"）→ agent_rules；纯陈述第三方要求（"<实体> 那边要求 X"）→ entities
- 提取 vs 不提取：持久性（"以后/每次/所有"等泛化标志）→ 提取；单次性（"这次/刚才/算了"）→ 不提取

## 已有规则
{existing_rules}

## 用户发言
{user_messages}

## 输出格式
- 有新指令：`{{"agent_rules": {{"content": "<合并后完整规则>"}}}}`
- 无新指令：`{{"agent_rules": {{}}}}`
""" + _COMMON_OUTPUT_RULES


# [DEPRECATED] Entities — 第三方客观事实
ENTITIES_EXTRACT_PROMPT = """从对话中提取关于【第三方】的客观事实。第三方指非用户本人、非用户所在组织、非 Agent 的对象（客户、联系人、对手、对标企业等）。

## 识别原则
任何关于第三方的可复用事实：
- 角色关系 / 业务与规模 / 偏好与风格
- 第三方的要求或规则（陈述式，如"<实体> 那边要求 X"）
- 对用户方的反馈、评价、承诺
- 竞争情报、时效性信息
- 用户对第三方的判断（主语"我"但内容描述的是第三方）

第三方主体识别：具名公司/人名/品牌、可锁定的代词（这家公司/他们）、关系动词（跟进/对接/评估/合作）的宾语。

## 排除规则
以下内容不属于本路，本路返空：
- 用户自身的履历/团队/公司特征（无关系动词串联到第三方当前事实）→ profile
- 用户自己的偏好 → preferences
- 命令 Agent 行为的持久指令（动作指向 Agent）→ agent_rules
- 疑问句 / 纯查询 / 操作指令（"帮我发邮件给 X"）→ 不提取
- CRM 系统已有的结构化字段值（金额/阶段/电话/邮箱）→ 不提取，仅保留客户名和人名作为定位锚点

## 边界判定
- entities vs profile：主语是第三方或描述第三方事实 → entities；主语是"我/我们"描述自身特征 → profile。特殊情况：纯履历提到公司名但无关系动词 → profile；履历+关系动词串联到第三方当前事实 → entities
- entities vs agent_rules：纯陈述第三方的要求/特点 → entities；命令 Agent 针对该实体采取行动 → agent_rules
- 提取 vs 不提取：增量认知（系统中查不到的洞察）→ 提取；系统数据字段值（可通过工具实时查询）→ 不提取

## 已有实体
{existing_entities}

## 对话（仅用户发言）
{conversation}

## 输出格式
```json
{{"entities": [{{"abstract": "<1 句>", "overview": "<Markdown>", "content": "<完整叙述>", "merge_key": "<如 公司/项目>", "parent_entity": "<父实体>"}}]}}
```
无有效实体返回 `{{"entities": []}}`。
""" + _COMMON_OUTPUT_RULES
