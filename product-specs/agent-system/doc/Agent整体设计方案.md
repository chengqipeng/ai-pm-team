# NeoAgent2.0 整体设计方案

> 面向 2B CRM SaaS 的企业级 Agent 系统完整设计。
> 融合 Claude Code + NeoAgents + OpenViking/Hermes 四大体系精华。
> 本文档是系统全貌概览，各子系统详细设计见独立文档（记忆 / 上下文压缩 / 内容审查 / 协议 / 数据库表 等）。

---

## 一、系统整体架构

### 1.1 架构总览

```
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 1 层 · 应用层                                                              │
│ ┌─────────────────────────────────┐  ┌─────────────────────────────────────┐ │
│ │ AI Agent 应用（对话式入口）       │  │ aPaaS 定制应用（表单/流程/仪表盘）   │ │
│ │ • 营销 Agent  • 销售助理 Agent   │  │ • 客户管理  • 商机管理  • 合同管理   │ │
│ │ • 客服 Agent  • 分析师 Agent     │  │ • 项目管理  • 审批中心              │ │
│ │ 入口: 对话窗口/语音/嵌入式浮窗   │  │ 入口: 菜单导航/工作台/移动端        │ │
│ └─────────────────────────────────┘  └─────────────────────────────────────┘ │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 2 层 · 业务组件层（aPaaS 专属，Agent 通过 Tool 调用）                       │
│   营销管理 │ 销售管理 │ 伙伴管理 │ 服务管理 │ 智能分析 │ 定制组件              │
│   边界：Agent 不拥有业务组件，通过 Tool 调用 aPaaS 组件暴露的 API              │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 3 层 · 工具与开发层                                                        │
│ ┌─────────────────────────────────┐  ┌─────────────────────────────────────┐ │
│ │ AI 配置工具（10 项）             │  │ aPaaS 核心开发能力（9 项）           │ │
│ │ • Agent 设计器  • Prompt 设计器  │  │ • 布局设计器  • UI 组件库            │ │
│ │ • Action 设计器 • 智能流程设计器 │  │ • 脚本代码    • 开发 SDK             │ │
│ │ • 知识库管理    • MCP 集成配置   │  │ • CI/CD       • NEX 扩展开发         │ │
│ │ • 测试评估工具  • 可视化调试     │  │ • 自定义页面  • IDE 插件             │ │
│ │ • UI 设计器(AI) • 元数据描述工具 │  │ • 自定义 API                        │ │
│ └─────────────────────────────────┘  └─────────────────────────────────────┘ │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 4 层 · 核心引擎层（最关键的分界线）                                         │
│ ┌─────────────────────────────────┐  ┌─────────────────────────────────────┐ │
│ │ AI 核心能力（12 项）             │  │ aPaaS 数据与流程引擎（10 项）        │ │
│ │ • 意图推理      • 智能流程编排   │  │ • 业务对象建模  • 字段映射           │ │
│ │ • 记忆系统      • 上下文工程     │  │ • 校验规则      • 工作流             │ │
│ │ • Agentic RAG   • Multi-Agent   │  │ • 审批流        • 自动流             │ │
│ │ • 元数据描述管理 • 安全信任系统  │  │ • 规则引擎      • 可视化业务流       │ │
│ │ • 可观测系统    • MCP 接入层     │  │ • 脚本引擎      • 自定义 API         │ │
│ │ • 测试评估系统  • 配置迁移       │  │                                     │ │
│ └─────────────────────────────────┘  └─────────────────────────────────────┘ │
│                                                                               │
│  Agent 是"大脑"（决定做什么、怎么做）    aPaaS 是"手脚+骨骼"（保证做得对、安全）│
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 4.5 层 · 资源点扣费层（AI 专属）                                            │
│   统一接入 · 路由 · 限流 · 降级 · 计费                                         │
│   用量计量 / 资源点兑换 / 配额管控（租户→应用→用户三级）/ 账单中心 / 扣费拦截   │
│   边界：支撑能力层与基础设施平台之间的计费网关                                  │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 5 层 · AI 专属能力层                                                        │
│ ┌─────────────────────────────────┐  ┌─────────────────────────────────────┐ │
│ │ AI 模型 + 原子能力（16 项）      │  │ 企业级集成能力（6 项，aPaaS 侧）     │ │
│ │ • LLM 推理      • 嵌入模型      │  │ • Open API     • Bulk API           │ │
│ │ • 多模态 VLM    • 向量数据库     │  │ • 数据转换     • 协议转换            │ │
│ │ • 智能网络检索  • 文档解析       │  │ • 集成编排     • 监控告警            │ │
│ │ • 语音识别(ASR) • AI 生图        │  │                                     │ │
│ │ • 热词库        • 记忆管理器     │  │                                     │ │
│ │ • 内容安全      • 效果评估       │  │                                     │ │
│ │ • LLM 链路追踪                   │  │                                     │ │
│ └─────────────────────────────────┘  └─────────────────────────────────────┘ │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 6 层 · AI Trust Layer（AI 专属）                                            │
│   PII 数据掩码 / Prompt 注入防护 / 输出验证 / 零数据保留 / 毒性检测            │
│   HITL 检查点（交叉：Agent 发起 + aPaaS 审批流执行）                           │
│   Plan Mode / 审计日志（交叉：Agent 产生 + 统一审计存储）/ LLM 链路追踪        │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 7 层 · 统一平台服务（AI + aPaaS 共享）                                      │
│   身份与认证（SSO）/ 权限与访问控制（Permission Sets）/ 数据管理               │
│   审计与合规 / 应用治理（版本/沙盒/灰度）/ 多端适配 / 零信任与 API 治理        │
│   边界：Agent 继承用户登录态，不能获得超越用户本身的权限                        │
+═══════════════════════════════════════════════════════════════════════════════+
│ 第 8 层 · 共同底座                                                             │
│   多租户隔离 / 元数据体系 / 微服务架构 / 中间件 / K8S / DevSecOps              │
+═══════════════════════════════════════════════════════════════════════════════+
```

### 1.2 Agent 框架内部架构

> 对应 1.3 完整链路结构图中涉及的各组件，展示 Agent 框架内部的层级关系与数据流向。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         用户请求（自然语言 / 语音 / 图片 / 文档）               │
└─────────────────────────────────────┬───────────────────────────────────────┘
                                      ▼
╔═════════════════════════════════════════════════════════════════════════════════╗
║  入口预处理层（server.py，LangGraph 之外）                                      ║
║  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                        ║
║  │ 标题生成      │  │ 内容审查      │  │ 查询改写      │                        ║
║  │ (规则+LLM)   │  │ (敏感词+语义) │  │ (记忆增强)   │                        ║
║  └──────────────┘  └──────────────┘  └──────────────┘                        ║
╠═════════════════════════════════════════════════════════════════════════════════╣
║  上下文准备层（before_agent 中间件，执行 1 次）                                 ║
║  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     ║
║  │ Tracing      │  │ Dangling     │  │ Input        │  │ Memory       │     ║
║  │ Middleware   │  │ ToolCall MW  │  │ Transform MW │  │ Middleware   │     ║
║  │ (上下文统计) │  │ (悬挂修复)   │  │ (PII脱敏)    │  │ (记忆检索)   │     ║
║  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘     ║
╠═════════════════════════════════════════════════════════════════════════════════╣
║  Agent Loop（LLM 推理循环，可执行 N 轮）                                       ║
║                                                                               ║
║  ┌─ before_model ──────────────────────────────────────────────────────────┐  ║
║  │  Summarization MW（上下文压缩）                                          │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                      ▼                                        ║
║  ┌─ LLM 推理 ─────────────────────────────────────────────────────────────┐  ║
║  │  ModelRouter → LLM API（豆包/DeepSeek/OpenAI/Claude/...）               │  ║
║  │  输出: 纯文本回复 | tool_use 调用指令                                    │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                      ▼                                        ║
║  ┌─ after_model ───────────────────────────────────────────────────────────┐  ║
║  │  SubagentLimit MW → LoopDetection MW → OutputValidation MW              │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                      ▼                                        ║
║  ┌─ wrap_tool_call（仅 tool_use 时触发）───────────────────────────────────┐  ║
║  │  Guardrail MW → ToolErrorHandling MW → Clarification MW                 │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                      ▼                                        ║
║  ┌─ 能力执行层 ────────────────────────────────────────────────────────────┐  ║
║  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────────┐    │  ║
║  │  │ Tool       │  │ Skill      │  │ Plugin     │  │ SubAgent       │    │  ║
║  │  │ (原子操作) │  │ (SOP编排)  │  │ (基础设施) │  │ (任务委派)     │    │  ║
║  │  │            │  │            │  │            │  │                │    │  ║
║  │  │ query_data │  │ inline:    │  │ memory     │  │ sales/service  │    │  ║
║  │  │ modify_data│  │  注入prompt│  │ search     │  │ analytics/     │    │  ║
║  │  │ analyze    │  │ fork:      │  │ notification│ │ config/data    │    │  ║
║  │  │ web_search │  │  子Agent   │  │ image_gen  │  │ research       │    │  ║
║  │  └────────────┘  └────────────┘  └────────────┘  └────────────────┘    │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
║                                                                               ║
║  ← 循环: tool_result 回注 → 下一轮 LLM 推理（直到纯文本回复）                 ║
╠═════════════════════════════════════════════════════════════════════════════════╣
║  回复输出                                                                      ║
║  ┌─────────────────────────────────────────────────────────────────────────┐  ║
║  │  SSE 流式推送 → AG-UI 事件流 → A2UI 动态组件渲染 → 前端                  │  ║
║  └─────────────────────────────────────────────────────────────────────────┘  ║
╠═════════════════════════════════════════════════════════════════════════════════╣
║  后处理层（after_agent 中间件，执行 1 次）                                      ║
║  ┌──────────────┐  ┌──────────────────────────────────────────────────────┐  ║
║  │ OutputRender │  │ Memory Middleware (after_agent)                       │  ║
║  │ MW           │  │ ┌────────────┐  ┌────────────┐  ┌────────────┐      │  ║
║  │ (PII还原+   │  │ │ 记忆提取    │  │ 记忆合并    │  │ 记忆反思    │      │  ║
║  │  格式化)     │  │ │ (4路并行)  │  │ (语义去重) │  │ (冲突检测) │      │  ║
║  └──────────────┘  │ └────────────┘  └────────────┘  └────────────┘      │  ║
║                     └──────────────────────────────────────────────────────┘  ║
╠═════════════════════════════════════════════════════════════════════════════════╣
║  数据存储层                                                                    ║
║  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  ║
║  │ LLM API  │  │ 向量数据库│  │PostgreSQL│  │  Redis   │  │ 对象存储     │  ║
║  │ (多厂商) │  │ (记忆检索)│  │(会话/记忆)│  │ (检查点) │  │ (文件/图片) │  ║
║  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────────┘  ║
╚═════════════════════════════════════════════════════════════════════════════════╝
```
