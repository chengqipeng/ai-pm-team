# 对比分析汇总

> 本文档汇总了三系统深度对比、apps-agent 记忆对比、Hermes-Agent 对比、V2-DeepAgent 差异分析四部分内容。

---

## 一、三系统深度对比分析

# DeepAgent vs Hermes Agent vs Claude Code — 深度对比分析

> 基于 DeepAgent 当前代码、Hermes Agent v0.10.0 源码结构、Claude Code 泄露源码分析。本文档整合了全维度代码级对比（架构/工具/技能/子Agent/中间件/配置）和上下文压缩专项深度对比（含 neo-apps 源码分析）。

---

# 第一部分：全维度代码级对比

---

## 一、架构定位对比

| 维度 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| 定位 | 2B CRM SaaS Agent 框架 | 自改进个人 AI Agent | 代码工程 Agentic CLI |
| 语言 | Python (LangChain) | Python (原生) | TypeScript (Bun + React Ink) |
| Agent 引擎 | `langchain.agents.create_agent` | 自建 `AIAgent` 循环 (`run_agent.py`) | 自建 `QueryEngine` (46K 行) |
| 工具数量 | 5 CRM 工具 + SkillsTool + AgentTool | 40+ 内置工具 | 17+ 核心工具 |
| 技能系统 | SKILL.md + inline/fork | SKILL.md + 自动生成 + agentskills.io 标准 | Prompt-native Skill + Plugin |
| 子 Agent | AgentTool + AgentFactory | delegate_tool + 隔离子 Agent | AgentTool + Coordinator/Worker + Mailbox |
| 记忆 | MemoryMiddleware + NoopEngine | 三层记忆（Session + FTS5 + User Model） | 三层压缩（Micro + Auto + Full Compact） |
| 开源 | 内部项目 | MIT 开源 (95.6K stars) | 商业产品（源码泄露分析） |

---

## 二、工具系统逐项对比

| 能力 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| 工具基类 | 自定义 `Tool` ABC + Pydantic `BaseTool` | 自建 registry + schema | 自建 Tool 基类 (~29K 行 TS) |
| 工具注册 | `ToolRegistry` + `ToolLoader`(缺) | `tools/registry.py` 中央注册 | 中央 Tool Registry |
| 动态描述 `description(input)` | ✅ `Tool.description(input_data)` | ❌ 静态描述 | ✅ 动态描述 |
| 延迟加载 `shouldDefer` | ✅ `Tool.should_defer` | ❌ | ✅ `shouldDefer + searchHint` |
| 搜索提示 `searchHint` | ✅ `Tool.search_hint` | ❌ | ✅ |
| 每工具结果预算 | ✅ `max_result_size_chars` (50K) | ❌ | ✅ `maxResultSizeChars` |
| 工具别名 | ✅ `Tool.aliases` | ❌ | ❌ |
| 只读/破坏性标记 | ✅ `is_read_only` / `is_destructive` | ❌ | ✅ 每工具权限级别 |
| 输入验证 | ✅ `validate_input()` | ❌ | ✅ Zod schema 验证 |
| 压缩协作 | ✅ `summary_threshold` / `summary_max_words` | ✅ `_summarize_tool_result` 按工具类型 | ✅ MicroCompact 本地裁剪 |
| 工具权限隔离 | ✅ `GuardrailMiddleware` 白名单 | ✅ `approval.py` 危险命令检测 | ✅ 每工具独立权限级别 |
| MCP 集成 | ❌ | ✅ `mcp_tool.py` (~1050 行) | ✅ MCP 服务器 + 指令注入 |
| 工具自动发现 | ❌ (缺 ToolLoader 目录扫描) | ✅ `discover_builtin_tools()` | ✅ 编译时注册 |

**DeepAgent 优势：** Tool 高级特性（aliases/search_hint/should_defer/max_result_size_chars/summary_threshold）是三者中最完整的元数据模型，Claude Code 有类似设计但 Hermes 缺失。

**DeepAgent 差距：** 缺 MCP 集成、缺工具自动发现（ToolLoader 目录扫描）、实际工具数量少（5 个 CRM 工具 vs Hermes 40+ vs Claude Code 17+）。

---

## 三、技能系统逐项对比

| 能力 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| 技能定义格式 | SKILL.md (YAML frontmatter + Markdown body) | SKILL.md (agentskills.io 标准) | Prompt-native + Plugin metadata |
| 技能加载 | `SkillLoader.discover()` 扫描目录 | `skill_commands.py` + Skills Hub | `loadPluginCommands.ts` |
| 技能注册 | `SkillRegistry` | 内存索引 + FTS5 搜索 | Plugin Registry |
| 技能执行 | `SkillExecutor` (inline/fork) | 直接注入 prompt | `SkillTool/prompt.ts` |
| inline 模式 | ✅ prompt 注入当前对话 | ✅ 等价 | ✅ 等价 |
| fork 模式 | ✅ 创建子 Agent 执行 | ✅ delegate_tool 子 Agent | ✅ AgentTool 子 Agent |
| 自动技能生成 | ❌ | ✅ 任务完成后自动创建 SKILL.md | ❌ |
| 技能自改进 | ❌ | ✅ 使用中自动优化 | ❌ |
| 技能市场 | ❌ | ✅ Skills Hub (浏览/安装) | ❌ |
| 技能安装器 | 占位 `NotImplementedError` | ✅ `installer.py` | ❌ |
| 技能验证 | ✅ `SkillLoader.validate()` | ✅ `validation.py` | ❌ (Plugin metadata 验证) |
| allowed_tools 约束 | ✅ 技能声明允许的工具列表 | ❌ | ❌ |
| 技能参数化 | ✅ `{arg}` 占位符替换 | ✅ | ❌ |

**DeepAgent 优势：** `allowed_tools` 约束（技能级工具白名单）是独有设计，适合 SaaS 多租户场景。inline/fork 双模式 + AgentFactory 缓存。

**DeepAgent 差距：** 缺自动技能生成（Hermes 的核心卖点）、缺技能自改进、缺技能市场。

---

## 四、子 Agent 系统逐项对比

| 能力 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| 子 Agent 工具 | `AgentTool` (Pydantic BaseTool) | `delegate_tool.py` | `AgentTool.tsx` |
| 子 Agent 工厂 | `AgentFactory` (LRU 缓存 + max_depth=3) | 直接 spawn 隔离进程 | 同一 Tool Registry 内 spawn |
| 嵌套深度限制 | ✅ max_depth=3 | ❌ (进程隔离天然限制) | ❌ (扁平架构) |
| 实例缓存 | ✅ LRU OrderedDict | ❌ (每次新建) | ❌ |
| 并发限制 | ✅ `SubagentLimitMiddleware` (max=3) | ❌ | ❌ |
| Coordinator/Worker | ❌ | ❌ | ✅ Mailbox 模式 + 原子 claim |
| 异步 fire-and-forget | ✅ `AsyncSubAgentManager` | ✅ 并行 workstream | ❌ |
| 子 Agent 配置 | `SubagentConfig` dataclass | 无独立配置 | 无独立配置 |
| 子 Agent 注册表 | `SubagentRegistry` | ❌ | ❌ |

**DeepAgent 优势：** AgentFactory LRU 缓存 + SubagentConfig 独立配置 + SubagentLimitMiddleware 并发限制，是三者中最完整的子 Agent 管理体系。

**DeepAgent 差距：** 缺 Coordinator/Worker Mailbox 模式（Claude Code 的危险操作审批机制）。

---

## 五、记忆与上下文压缩逐项对比

| 能力 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| **Session 记忆** | `SummarizationMiddleware` (token 估算 → 摘要) | Session memory (标准上下文管理) | MicroCompact (本地裁剪，0 API 调用) |
| **持久化记忆** | `MemoryMiddleware` + `NoopMemoryEngine` (协议已定义) | FTS5 全文搜索 (~10ms/10K 文档) | 无独立持久化（依赖 context files） |
| **用户画像** | `MemoryDimension.USER_PROFILE` (协议已定义) | 自动 User Model (跨 session 偏好) | 无 |
| **维度化检索** | ✅ 4 维度 (user_profile/customer_context/task_history/domain_knowledge) | ❌ 扁平检索 | ❌ |
| **向量检索** | ❌ (NoopEngine 占位) | ❌ (FTS5 关键词) | ❌ |
| **记忆提取** | `MemoryMiddleware.aafter_agent` 异步提取 | 自动 skill 生成 + 记忆持久化 | 无自动提取 |
| **压缩层级** | 1 层 (SummarizationMiddleware) | 1 层 (context compression) | 3 层 (Micro + Auto + Full Compact) |
| **压缩触发** | token 估算 > 75% 阈值 | 接近上下文窗口 | 接近上下文窗口 + 13K buffer |
| **压缩熔断** | ❌ | ❌ | ✅ 3 次失败后停止 |
| **压缩后文件重注入** | ❌ | ❌ | ✅ 最近访问文件 (≤5K tokens/file) |
| **压缩后预算重置** | ❌ | ❌ | ✅ 重置到 50K tokens |

**DeepAgent 优势：** 维度化记忆检索（4 维度）是独有设计，适合 CRM 场景（用户画像 + 客户上下文 + 任务历史 + 领域知识）。MemoryEngine 协议设计良好，可插拔。

**DeepAgent 差距：** 压缩只有 1 层（vs Claude Code 3 层），缺压缩熔断、缺压缩后文件重注入、缺 FTS5 全文搜索、MemoryEngine 仍是 Noop 占位。

---

## 六、中间件系统逐项对比

| 中间件 | DeepAgent | Hermes Agent | Claude Code |
|--------|-----------|-------------|-------------|
| 工具错误处理 | ✅ `ToolErrorHandlingMiddleware` | ✅ 内置 | ✅ 内置 |
| 悬空工具调用修复 | ✅ `DanglingToolCallMiddleware` | ❌ | ❌ |
| 安全护栏 | ✅ `GuardrailMiddleware` | ✅ `approval.py` | ✅ 每工具权限 |
| 循环检测 | ✅ `LoopDetectionMiddleware` (hash + 滑动窗口) | ❌ | ❌ (QueryEngine 内置) |
| 上下文压缩 | ✅ `SummarizationMiddleware` | ✅ `context_compressor.py` | ✅ 三层压缩 |
| 日志追踪 | ✅ `AgentLoggingMiddleware` | ❌ (内置日志) | ✅ 遥测系统 |
| 澄清中断 | ✅ `ClarificationMiddleware` | ❌ | ❌ (Hooks 系统) |
| 记忆注入 | ✅ `MemoryMiddleware` | ✅ 内置 | ❌ (context files) |
| 输出验证 | ✅ `OutputValidationMiddleware` | ❌ | ❌ |
| 子 Agent 限制 | ✅ `SubagentLimitMiddleware` | ❌ | ❌ |
| 输入转换 | ✅ `InputTransformMiddleware` | ❌ | ❌ |
| 标题生成 | ✅ `TitleMiddleware` | ❌ | ❌ |
| 计划模式 | ✅ `TodoMiddleware` | ❌ | ✅ Task tracking |
| Prompt 缓存 | ❌ | ❌ | ✅ `prompt_caching.py` (Anthropic) |
| 遥测指标 | ❌ | ❌ | ✅ 挫败感指标 + continue 计数 |
| Hooks 系统 | ❌ | ❌ | ✅ 17 个生命周期事件 |

**DeepAgent 优势：** 13 个独立中间件，全部继承 `AgentMiddleware`，是三者中最模块化的中间件栈。DanglingToolCall、LoopDetection、Clarification、OutputValidation、SubagentLimit 是独有的。

**DeepAgent 差距：** 缺 Prompt 缓存、缺遥测指标、缺 Hooks 系统（Claude Code 的 17 个生命周期事件）。

---

## 七、配置与部署对比

| 能力 | DeepAgent | Hermes Agent | Claude Code |
|------|-----------|-------------|-------------|
| 配置格式 | Python dataclass 硬编码 | TOML (`~/.hermes/config.toml`) | JSON + 环境变量 |
| 环境变量覆盖 | ❌ | ✅ | ✅ |
| 模型切换 | 代码修改 | `hermes model` 命令 | `/model` 命令 |
| 多模型路由 | ❌ | ✅ (辅助 LLM 按任务选模型) | ❌ (单模型) |
| 部署方式 | Python 包 | CLI + Docker + VPS + Serverless | npm 包 |
| 消息平台 | 无 | 6 个 (Telegram/Discord/Slack/WhatsApp/Signal/CLI) | CLI only |
| 定时任务 | ❌ | ✅ 内置 cron 调度 | ❌ |
| Feature Flags | ❌ | ❌ | ✅ 108 个编译时特性门控 |

---

## 八、独有能力汇总

### DeepAgent 独有
1. **Plugin 生命周期体系** — platform/industry/tenant 三层分级，register → initialize → shutdown
2. **CRM 业务模拟** — 完整的内存数据库 + CRUD + 聚合 + 5 个业务工具 + 6 个业务技能
3. **Tool 元数据模型** — aliases/search_hint/should_defer/max_result_size_chars/summary_threshold/code_extractable
4. **维度化记忆** — 4 维度检索（user_profile/customer_context/task_history/domain_knowledge）
5. **allowed_tools 技能约束** — 技能级工具白名单
6. **SubagentConfig 独立配置** — 每个子 Agent 可配置独立的 middleware/tools/skills
7. **MockLLMClient** — 可编程测试（预设工具调用响应脚本）
8. **ServiceBackend 协议** — 微服务调用抽象层

### Hermes Agent 独有
1. **自动技能生成** — 任务完成后自动创建 SKILL.md（核心卖点）
2. **技能自改进** — 使用中自动优化技能
3. **Skills Hub** — 技能市场（浏览/安装/118 个内置技能）
4. **三层记忆** — Session + FTS5 持久化 + 自动 User Model
5. **多模型路由** — 辅助 LLM 按任务类型选模型
6. **6 平台消息网关** — Telegram/Discord/Slack/WhatsApp/Signal/CLI
7. **内置 cron 调度** — 自然语言定时任务
8. **40+ 内置工具** — 终端/文件/Web/浏览器/代码执行/MCP
9. **agentskills.io 开放标准** — 技能互操作
10. **RL 训练集成** — Atropos 环境 + 轨迹压缩

### Claude Code 独有
1. **三层上下文压缩** — MicroCompact(0 API) + AutoCompact(13K buffer) + FullCompact(重置 50K)
2. **压缩熔断** — 3 次失败后停止
3. **Coordinator/Worker Mailbox** — 危险操作审批 + 原子 claim
4. **17 个 Hooks 生命周期事件** — 工具调用前后、Agent 启停等
5. **Prompt 缓存** — Anthropic 原生缓存
6. **遥测指标** — 挫败感指标（swearing frequency）+ continue 计数
7. **108 个 Feature Flags** — 编译时死代码消除
8. **46K 行 QueryEngine** — 自愈查询循环 + 流式 + 重试
9. **每工具独立权限级别** — BashTool vs FileReadTool 不同权限
10. **压缩后文件重注入** — 最近访问文件 ≤5K tokens/file

---

## 九、DeepAgent 应吸收的精华（按优先级）

### P0 — 必须吸收

| # | 来源 | 能力 | 理由 |
|---|------|------|------|
| 1 | Claude Code | **三层压缩** — 增加 MicroCompact（本地裁剪旧工具输出，0 API 调用） | 当前只有 1 层，长 session 会崩 |
| 2 | Claude Code | **压缩熔断** — 连续 N 次压缩失败后停止 | 防止无限压缩循环 |
| 3 | Hermes | **MCP 集成** — 连接外部 MCP 服务器扩展工具 | 生态互操作必备 |
| 4 | Hermes | **ToolLoader 目录自动发现** — 扫描 tools/ 目录加载 BaseTool 子类 | 当前缺失，v2 已有 |

### P1 — 重要优化

| # | 来源 | 能力 | 理由 |
|---|------|------|------|
| 5 | Claude Code | **Prompt 缓存** — 缓存不变的 system prompt 部分 | 降低 token 成本 |
| 6 | Hermes | **多模型路由** — 辅助 LLM 按任务类型选模型 | 成本优化 40-60% |
| 7 | Hermes | **FTS5 全文搜索** — 替代 NoopEngine 的持久化记忆 | 记忆系统从占位到可用 |
| 8 | Claude Code | **压缩后文件重注入** — Full Compact 后重注入最近访问文件 | 保持上下文连贯性 |
| 9 | Hermes | **自动技能生成** — 任务完成后自动创建 SKILL.md | 自改进能力 |

### P2 — 锦上添花

| # | 来源 | 能力 | 理由 |
|---|------|------|------|
| 10 | Claude Code | **Coordinator/Worker Mailbox** — 危险操作审批 | 多 Agent 安全 |
| 11 | Claude Code | **遥测指标** — 挫败感 + continue 计数 | 产品质量监控 |
| 12 | Hermes | **技能自改进** — 使用中优化技能 | 长期价值 |
| 13 | Hermes | **cron 调度** — 自然语言定时任务 | 自动化场景 |
| 14 | Claude Code | **Hooks 系统** — 生命周期事件钩子 | 可扩展性 |

---

# 第二部分：上下文压缩专项深度对比

---

## 一、三系统定位与压缩目标差异

| 维度 | Claude Code | Hermes Agent | neo-apps-ai-agent-service |
|------|------------|--------------|--------------------------|
| 产品形态 | 终端编码助手 | 通用持久 Agent 运行时 | 2B CRM 业务 Agent（LangGraph + FastAPI） |
| 典型上下文窗口 | 200K tokens | 128K-200K tokens | 取决于接入的模型（豆包/通义等） |
| 数据特征 | 代码文件、grep 结果、bash 输出 | 通用对话 + 工具结果 | CRM 业务数据 + 前端标准 UI 组件 |
| 压缩核心矛盾 | 代码文件多且大，工具调用频繁 | 长会话跨 session 记忆保持 | 工具结果是 UI 组件数据，不需要全量进入 LLM 上下文 |
| 设计哲学 | "尽可能推迟，尽可能便宜，逐级升级" | "双层压缩 + 可插拔引擎" | "源头隔离：工具结果走前端组件渲染，LLM 上下文只存 query + answer 摘要" |

---

## 二、架构层次对比

### 2.1 Claude Code：五层级联（Five-Layer Cascade）

Claude Code 的压缩是业界最精细的工程实现，核心思想是**按成本递增逐级触发**：

```
Layer 0: 大结果持久化到磁盘
  ↓ 仍然超限
Layer 1: Cached Microcompact（利用 API cache_edits 删除服务端缓存中的旧工具结果）
  ↓ 仍然超限
Layer 2: Time-Based Microcompact（空闲 >60min 后清理本地消息内容）
  ↓ 仍然超限
Layer 3: Session Memory Compact（利用后台持续维护的结构化笔记作为摘要，零 LLM 成本）
  ↓ 仍然超限
Layer 4: Full Compact（Fork Agent 调用 LLM 生成完整摘要）
```

**关键设计决策：**

1. **Layer 0 — 磁盘持久化**：工具结果超过 50K 字符时写入磁盘，上下文只保留 2KB 预览 + 文件路径。Read 工具豁免（避免循环依赖）。单条消息所有 tool_result 总量上限 200K 字符。

2. **Layer 1 — Cached Microcompact（最精妙的设计）**：利用 Anthropic API 的 `cache_edits` 能力，直接在服务端缓存中删除旧工具结果，**本地消息完全不修改**。prompt cache 前缀不受影响，几乎零成本。

3. **Layer 2 — Time-Based Microcompact**：用户离开超过 60 分钟后（服务端缓存已过期），直接修改本地消息内容清理旧工具结果。与 Layer 1 互斥。

4. **Layer 3 — Session Memory Compact（最创新的设计）**：后台 Fork Agent 持续维护一份结构化 Markdown 笔记（11 个 section）。压缩时直接用这份笔记作为摘要，**零 LLM 调用成本**。

5. **Layer 4 — Full Compact**：最后手段。通过 Fork Agent 共享主会话的 prompt cache 前缀。摘要 prompt 使用两阶段结构：`<analysis>` 思考块 + `<summary>` 正式摘要（9 个 section）。

**触发阈值**：200K 窗口约 167K 触发。熔断器：连续 3 次失败后停止重试。

### 2.2 Hermes Agent：双层压缩 + 可插拔引擎（源码深度分析）

> 源码位置：`agent/context_compressor.py`（53KB）、`agent/context_engine.py`、`agent/prompt_caching.py`、`agent/auxiliary_client.py`

```
Gateway Session Hygiene（85% 阈值，安全网，粗估 token）
  ↓
Agent ContextCompressor（50% 阈值，主压缩器，真实 API token 计数）
  Phase 1: 工具结果智能裁剪（3 遍扫描：去重 → 信息摘要替换 → 参数截断）
  Phase 2: 确定边界（head 保护 + token 预算 tail 保护 + 边界对齐 + 最后用户消息锚定）
  Phase 3: 生成结构化摘要（12 section 模板，迭代更新，focus topic 支持）
  Phase 4: 组装 + 角色交替修复 + 孤立 tool 对清理 + 反抖动保护
```

**精华 1：三遍工具结果裁剪（`_prune_old_tool_results`）**

这是 Hermes 最精细的设计之一——不是简单地用占位符替换旧结果，而是分三遍处理：

- **Pass 1 — 去重**：对所有 tool_result 内容做 MD5 哈希，相同内容只保留最新的一份，旧的替换为 `[Duplicate tool output — same content as a more recent call]`。解决"同一个文件被读 5 次"的问题。
- **Pass 2 — 信息摘要替换**：对保护区外的旧 tool_result（>200 字符），不是用通用占位符，而是调用 `_summarize_tool_result()` 生成**信息丰富的一行摘要**。例如：
  - `[terminal] ran 'npm test' -> exit 0, 47 lines output`
  - `[read_file] read config.py from line 1 (1,200 chars)`
  - `[search_files] content search for 'compress' in agent/ -> 12 matches`
  - `[write_file] wrote to main.py (35 lines)`
  - `[web_search] query='hermes agent context' (3,400 chars result)`
  - 支持 20+ 种工具的专用摘要格式，每种都提取最有价值的信息（命令、退出码、文件路径、匹配数等）
- **Pass 3 — 参数截断**：对保护区外的 assistant 消息中的 tool_call arguments（如 write_file 的 50KB content 参数），截断到 200 字符 + `...[truncated]`。

**保护区边界**使用 token 预算（而非固定消息数）从末尾向前累积，同时保证最少 N 条消息。

**精华 2：Token 预算 Tail 保护（`_find_tail_cut_by_tokens`）**

不是简单的"保留最近 20 条"，而是：
- 从消息列表末尾向前累积 token，直到达到 `tail_token_budget`（默认 = `threshold_tokens × 0.20`，200K 模型约 20K tokens）
- 硬性最少保护 3 条消息
- 允许超出预算 1.5 倍以避免在超大消息中间切割
- **边界对齐**：`_align_boundary_backward()` 确保不拆分 tool_call/tool_result 组——如果切割点落在连续 tool_result 中间，向前回退到父 assistant 消息
- **最后用户消息锚定**（`_ensure_last_user_message_in_tail`，修复 #10896）：确保最近的 user 消息始终在 tail 中，否则 LLM 会丢失当前任务

**精华 3：12-Section 结构化摘要模板**

摘要模板比之前分析的 7 section 更丰富，实际有 12 个 section：

```
## Active Task          ← 最重要的字段：逐字复制用户最近的未完成请求
## Goal                 ← 用户的整体目标
## Constraints & Preferences
## Completed Actions    ← 编号列表，格式：N. ACTION target — outcome [tool: name]
## Active State         ← 工作目录、分支、修改的文件、测试状态、运行中的进程
## In Progress          ← 压缩触发时正在进行的工作
## Blocked              ← 未解决的阻塞/错误，包含精确错误消息
## Key Decisions        ← 重要技术决策及原因
## Resolved Questions   ← 已回答的用户问题（含答案，防止下一个 assistant 重复回答）
## Pending User Asks    ← 未回答的用户请求
## Relevant Files       ← 读取/修改/创建的文件
## Remaining Work       ← 剩余工作（作为上下文，不是指令）
## Critical Context     ← 具体值、错误消息、配置细节
```

关键设计：
- `Active Task` 被标注为"THE SINGLE MOST IMPORTANT FIELD"——要求逐字复制用户最近的未完成请求
- `Completed Actions` 要求精确格式：`N. ACTION target — outcome [tool: name]`，包含文件路径、命令、行号
- `Resolved Questions` 防止下一个 assistant 重复回答已解决的问题
- `Remaining Work` 刻意用"remaining"而非"next steps"，避免被 LLM 误读为主动指令

**精华 4：迭代摘要更新**

后续压缩时，将上次摘要 + 新增对话一起发给 LLM，要求：
- PRESERVE 所有仍然相关的信息
- ADD 新完成的 action 到编号列表（继续编号）
- MOVE 已完成的项从 "In Progress" 到 "Completed Actions"
- MOVE 已回答的问题到 "Resolved Questions"
- UPDATE "Active State" 反映当前状态
- CRITICAL: 更新 "Active Task" 为用户最近的未完成请求

存储在 `self._previous_summary` 中，跨压缩周期保持。

**精华 5：摘要 Prompt 的"隔离"设计**

```python
SUMMARY_PREFIX = (
    "[CONTEXT COMPACTION — REFERENCE ONLY] Earlier turns were compacted "
    "into the summary below. This is a handoff from a previous context "
    "window — treat it as background reference, NOT as active instructions. "
    "Do NOT answer questions or fulfill requests mentioned in this summary; "
    "they were already addressed. "
    "Your current task is identified in the '## Active Task' section..."
)
```

摘要器 preamble 借鉴了 OpenCode 的"do not respond to any questions"和 Codex 的"different assistant"设计：
- 告诉摘要器它是一个"summarization agent creating a context checkpoint"
- 输出将注入给一个"DIFFERENT assistant"
- 不要回答任何问题，只输出结构化摘要

**精华 6：Focus Topic 引导压缩**

支持 `/compress <topic>` 命令（借鉴 Claude Code 的 `/compact`）：
- 当用户指定 focus topic 时，摘要器将 60-70% 的 token 预算分配给相关内容
- 不相关的内容更激进地压缩（一行摘要或省略）

**精华 7：反抖动保护（Anti-Thrashing）**

```python
def should_compress(self, prompt_tokens=None):
    if self._ineffective_compression_count >= 2:
        logger.warning("Compression skipped — last %d compressions saved <10%% each.")
        return False
```

如果连续 2 次压缩每次节省 <10%，跳过后续压缩。防止"每次只删 1-2 条消息"的无限循环。

**精华 8：摘要失败冷却**

- 无 provider 可用：冷却 600 秒（10 分钟）
- 瞬态错误（超时/限流）：冷却 60 秒
- 摘要模型不可用（404/503）：自动降级到主模型重试
- 冷却期间跳过摘要，middle 段直接丢弃（有损但不阻塞）

**精华 9：角色交替修复**

压缩后摘要消息的 role 选择避免与前后消息同 role（API 要求交替）。如果无论选 user 还是 assistant 都会冲突，则将摘要合并到 tail 的第一条消息中，用分隔符隔开。

**精华 10：可插拔引擎 ABC**

`ContextEngine` 定义了完整的生命周期接口：
- `should_compress()` / `compress()` — 核心压缩
- `on_session_start()` / `on_session_end()` / `on_session_reset()` — 会话生命周期
- `get_tool_schemas()` / `handle_tool_call()` — 引擎可以暴露工具（如 LCM 的 `lcm_grep`）
- `update_model()` — 模型切换时重新计算预算
- `get_status()` — 状态展示

**精华 11：Prompt Caching（`system_and_3` 策略）**

4 个 `cache_control` 断点：system prompt + 最近 3 条非 system 消息的滚动窗口。支持 5m / 1h TTL。处理 string/list/None/tool 四种 content 格式。

**精华 12：辅助 LLM 路由（`auxiliary_client.py`）**

摘要调用通过 `call_llm(task="compression")` 路由，支持：
- 7 级 provider 自动检测链（OpenRouter → Nous → Custom → Codex → API-key providers）
- 按 task 配置独立的 provider/model/timeout（`auxiliary.compression.model`）
- 402 支付错误自动降级到下一个 provider
- 连接错误自动降级
- 客户端缓存 + 事件循环感知（防止 async 客户端跨 loop 使用）


### 2.3 neo-apps-ai-agent-service：源头隔离 + 虚拟文件 + 摘要回写

> 源码位置：`repos/apass_old_projects/neo-apps-ai-agent-service/service/neo_agent/`（新版 Agent，2025-12-10 起）

neo-apps 的上下文管理与 Claude Code / Hermes **有本质区别**——它不是在"已经膨胀的上下文中做压缩"，而是**从源头控制什么内容进入 LLM 上下文**。

```
┌─────────────────────────────────────────────────────────────┐
│ 核心架构：工具结果走前端组件，不进入 LLM 历史上下文            │
│                                                              │
│ 当前轮次（LangGraph State 内）:                               │
│   工具结果 → process_sub_agent_result()                      │
│     ├→ 结果 ≤500 字符: 原文作为 ToolMessage 回传 reasoning   │
│     └→ 结果 >500 字符: 调用 LLM 生成摘要 → 摘要回传 reasoning│
│                         原文存入 FileInfo（虚拟文件）          │
│   工具结果 → 前端 UI 组件渲染（CustomContent/TextContent）    │
│     └→ 按标准组件 schema 组合概要数据，推送给前端展示          │
│                                                              │
│ 历史上下文（跨轮次）:                                         │
│   数据库存储: query + answer + answerSummary                  │
│   加载历史时（_init_neo_agent_state）:                        │
│     conversation_history = history_summary_message            │
│       → user: query / assistant: answerSummary（优先）或 answer│
│     file_list 虚拟文件:                                       │
│       → content = 完整 answer / summary = answerSummary       │
│   工具调用过程: ❌ 完全不进入历史上下文                        │
└─────────────────────────────────────────────────────────────┘
```

**关键设计决策（基于新版 Agent 源码分析）：**

1. **历史消息加载（`graph.py:_init_neo_agent_state` + `main.py:_init_history`）**：
   - 从数据库 `query_message` 加载最近 5 轮对话（`pageSize=5`）
   - 构建两套历史：
     - `history_summary_message`：user 的 query + assistant 的 `answerSummary`（优先）或完整 `answer` → 传入 `NeoAgentState.conversation_history`
     - `real_messages`：user 的 query + assistant 的**完整 answer** → 用于构建虚拟文件
   - `_init_neo_agent_state` 将 `real_messages` 转为 `FileInfo` 虚拟文件列表：
     - `file_path="/conversation_history"`
     - `content` = 完整 answer（供需要时引用原文）
     - `summary` = answerSummary（供意图识别等场景使用摘要）
     - `extend.query` = 用户问题，`extend.files` = 关联文件
   - **工具调用过程（tool_call / tool_result）完全不进入历史上下文**

2. **当前轮次的工具结果处理（`node.py:process_sub_agent_result`）**：
   - 子 Agent 执行完后，遍历结果中的 `TextContent` 和 `CustomContent`
   - `CustomContent`（UI 组件）：尝试 `template2str()` 或 `schema2str()` 转为文本
   - 当结果 >500 字符且非 `return_direct` 时，调用 `agent_summary_model()` 生成摘要
   - 摘要作为 `ToolMessage` 回传给 reasoning 循环
   - 原文存入 `TaskResult.original_text`

3. **虚拟文件系统（`state.py:FileInfo` + `node.py:call_sub_agent_after_node`）**：
   - 子 Agent 执行结果被包装为 `FileInfo` 对象存入 `NeoAgentState.file_list`
   - `FileInfo` 包含 `content`（原文）、`summary`（摘要）、`file_path`（如 `/action_result`）
   - 历史对话也被转为虚拟文件（`file_path="/conversation_history"`），用于意图识别
   - 虚拟文件在当前会话内跨步骤引用，但**不持久化到磁盘**，会话结束即消失

4. **回复摘要回写（`node.py:answer_question_node`）**：
   - `update_answer_summary`：当最终回复 >500 字符时，调用 `agent_summary_model()` 生成摘要
   - 摘要写入 `message_.answerSummary` 字段，持久化到数据库
   - 下次加载历史时，`history_summary_message` 优先使用 `answerSummary`
   - 这是 neo-apps 的**核心压缩机制**——不是压缩已有上下文，而是在存储时就生成摘要

5. **前端 UI 组件与上下文的关系**：
   - 工具结果通过 `neo_ai_emit_custom`（标准 UI 组件）和 `neo_ai_emit_message`（文本）推送给前端
   - 组件按 `schema` + `propsData` 渲染（如 BANT 分析卡片、客户画像卡片、音频文件卡片）
   - **组件数据不进入 LLM 上下文**——前端展示和 LLM 上下文是两条独立的数据通道

6. **意图识别中的历史组装（`node.py:get_task_list_impl`）**：
   - 从 `file_list` 中提取 `file_path="/conversation_history"` 的虚拟文件
   - 组装为 `user:{query}\nassistant:{summary}` 格式
   - 只使用摘要，不使用完整回复

7. **reasoning 循环中的历史组装（`node.py:reasoning_node`）**：
   - `messages_ = [SystemMessage, *history, *messages]`
   - `history` = `state.conversation_history`（即 `history_summary_message`，answerSummary 优先）
   - `messages` = 当前轮次的消息链（包含 ToolMessage 摘要）

**实际代码中不存在的东西：**
- ❌ 没有 SQLite/DuckDB/内存数据库
- ❌ 没有长期记忆存储系统（记忆模块 2026-04 开始开发，仍在迭代中）
- ❌ 没有 Microcompact / snip_history 等规则压缩
- ❌ 没有 prompt cache 优化
- ❌ 没有迭代摘要
- ❌ 没有可插拔压缩引擎

---

## 三、核心机制逐项对比

### 3.1 工具结果管理

| 机制 | Claude Code | Hermes Agent | neo-apps |
|------|------------|--------------|----------|
| 工具结果在上下文中的存在形式 | 完整保留在消息链中，通过多层压缩逐步清理 | 完整保留在消息链中，通过四阶段压缩清理 | **当前轮次**：>500 字符的结果由 LLM 生成摘要后作为 ToolMessage 回传；**历史轮次**：工具结果完全不进入历史上下文 |
| 大结果处理 | 超 50K 写磁盘，保留 2KB 预览 | 无专门处理 | >500 字符调用 LLM 摘要；原文存入 FileInfo 虚拟文件供当前会话引用 |
| 旧结果清理 | cache_edits 删服务端缓存 | >200 字符替换为占位符 | 不需要清理——工具结果本身不进入历史上下文 |
| 前端展示 | 终端文本输出 | 终端文本输出 | 标准 UI 组件（CustomContent）渲染，组件数据与 LLM 上下文分离 |

### 3.2 历史上下文管理

| 机制 | Claude Code | Hermes Agent | neo-apps |
|------|------------|--------------|----------|
| 历史存储内容 | 完整消息链（user + assistant + tool_call + tool_result） | 完整消息链 | **只存 query + answer + answerSummary**，工具过程不存储 |
| 历史加载方式 | 从内存/checkpoint 加载完整消息链 | 从内存加载完整消息链 | 从数据库加载最近 5 轮；构建两套：`conversation_history`（answerSummary）+ 虚拟文件（完整 answer + answerSummary） |
| assistant 回复处理 | 完整保留 | 完整保留 | 虚拟文件 content 保留完整 answer；`conversation_history` 和意图识别使用 answerSummary |
| 压缩时机 | 上下文接近窗口限制时 | 上下文超过 50% 阈值时 | **存储时就压缩**——answer 写入数据库时同步生成 answerSummary |

### 3.3 摘要生成策略

| 机制 | Claude Code | Hermes Agent | neo-apps |
|------|------------|--------------|----------|
| 零成本摘要 | Session Memory（后台持续维护笔记） | 无 | 无 |
| LLM 摘要触发 | Full Compact（上下文超限时） | Phase 3（上下文超 50% 时） | **两个触发点**：① 子 Agent 结果 >500 字符时摘要（`process_sub_agent_result`）；② 最终回复 >500 字符时生成 answerSummary（`answer_question_node`） |
| 摘要用途 | 替换被压缩的消息段 | 替换被压缩的 middle 段 | ① 工具结果摘要回传 reasoning 循环；② answerSummary 存入数据库供下次历史加载 |
| 迭代更新 | 无（Session Memory 是增量的） | 有（传入上次摘要要求更新） | 无 |

### 3.4 缓存策略

| 机制 | Claude Code | Hermes Agent | neo-apps |
|------|------------|--------------|----------|
| Prompt Cache | 极致优化（Cached MC + Fork 共享缓存） | system_and_3 策略 | 无 |
| 状态持久化 | 内存 + 磁盘 transcript | 内存 | Redis（LangGraph AsyncRedisSaver，TTL 24h） |

---

## 四、代码实现对比

### 4.1 Claude Code 核心实现（逆向分析）

```
核心调度逻辑:
  autoCompactIfNeeded(messages, context):
    if circuit_breaker.tripped: return
    if token_count < threshold: return
    result = try_session_memory_compact(messages)  // 零成本
    if result: return result
    run_microcompact(messages)  // Layer 1 or 2
    result = try_full_compact(messages)  // Layer 4, LLM 摘要
    if result: circuit_breaker.reset()
    else: circuit_breaker.increment()
```

### 4.2 Hermes Agent 核心实现

```python
class ContextCompressor(ContextEngine):
    def compress(self, messages, current_tokens=None):
        messages = self._prune_old_tool_results(messages)       # Phase 1
        head, middle, tail = self._determine_boundaries(messages) # Phase 2
        summary = self._generate_summary(middle, self._previous_summary) # Phase 3
        return self._assemble(head, summary, tail)              # Phase 4
```

### 4.3 neo-apps-ai-agent-service 核心实现（新版 Agent）

```python
# ===== 历史加载（graph.py:_init_neo_agent_state）=====
# 新版 Agent 的入口：将 _init_history 的结果转为 NeoAgentState

async def _init_neo_agent_state(request, history):
    file_list = []
    if history[2]:  # real_messages: user query + assistant 完整 answer
        file_info = None
        for idx, message_ in enumerate(history[2]):
            if message_['role'] == 'user':
                file_info = FileInfo(
                    file_path="/conversation_history",
                    file_name=f"history_{idx + 1}.md",
                    description=USER_RESULT.format(message_.get('content', ''))
                )
                file_info.extend = {
                    "query": message_.get('content', ""),
                    "files": message_.get("files", [])
                }
            elif message_['role'] == 'assistant' and file_info:
                file_info.content = message_.get('content', "")  # 完整 answer
                # history[5] = history_summary_message: answerSummary 优先
                file_info.summary = history[5][idx].get('content', "") if history[5] and history[5][idx] else ''
                file_list.append(file_info)

    return NeoAgentState(
        messages=[],
        user_input=get_message_text(request.messages[-1]),
        deep_num=1,
        conversation_history=history[5],  # history_summary_message（answerSummary 优先）
        file_list=file_list,              # 虚拟文件（content=完整answer, summary=answerSummary）
        # ...
    ).model_dump()

# ===== _init_history 中新版 Agent 的分支 =====
# history[2] = real_messages, history[5] = history_summary_message

for _message in messages:
    if _message.answer and _message.query:
        # history_summary_message: answerSummary 优先
        history_summary_message.append({'role': 'user', 'content': _message.query})
        history_summary_message.append({'role': 'assistant',
            'content': _message.answerSummary if _message.answerSummary else _message.answer})
        # real_messages: 新版 Agent 用完整 answer（不截断）
        real_messages.append({'role': 'user', 'content': _message.query, 'files': ...})
        real_messages.append({'role': 'assistant', 'content': _message.answer})  # 完整 answer

# ===== 工具结果摘要（node.py:process_sub_agent_result）=====

async def process_sub_agent_result(sub_agent_result, state):
    original_text = ""
    for item in sub_agent_result.result:
        if isinstance(item, TextContent):
            original_text += item.content
        elif isinstance(item, CustomContent):
            if template_str := item.template2str():
                original_text += template_str
            elif schema_str := item.schema2str():
                original_text += schema_str
            else:
                original_text += f"\n组件<{item.render_type}>:{str(item.data)}\n"

    summary_content = original_text
    if len(original_text) > 500 and not state.sub_agent_result.return_direct:
        model = agent_summary_model()
        response = await model.ainvoke([HumanMessage(content=SUMMARY_PROMPT.format(...))])
        summary_content = response.content

    return original_text, summary_content  # 原文 + 摘要

# ===== 虚拟文件存储（node.py:call_sub_agent_after_node）=====

async def call_sub_agent_after_node(state, config):
    original_text, summary = await process_sub_agent_result(sub_agent_result, state)

    # 摘要作为 ToolMessage 回传 reasoning 循环
    # 原文存入 FileInfo 虚拟文件
    file_info = FileInfo(file_path="/action_result", file_name=file_name,
                         content=original_text, summary=summary, file_size=len(original_text))
    file_list.append(file_info)

    return {
        "messages": ... + [ToolMessage(content=index_summary, tool_call_id=...)],
        "file_list": file_list,
    }

# ===== 回复摘要回写（node.py:answer_question_node）=====

async def answer_question_node(state, config):
    async def update_answer_summary(content):
        if len(content) > 500:
            summary_model = agent_summary_model()
            summary_response = await summary_model.ainvoke([
                HumanMessage(content=ANSWER_QUESTION_SUMMARY_PROMPT.format(...))])
            message_.answerSummary = summary_response.content  # 写入数据库
    # ... 生成回复后 ...
    await update_answer_summary(response)
```

**新版 Agent 的上下文管理流程图：**

```
用户消息 → _init_history 加载最近 5 轮
  ↓
_init_neo_agent_state 构建初始状态:
  conversation_history = [user:query, assistant:answerSummary] （reasoning 用）
  file_list = [FileInfo(content=完整answer, summary=answerSummary)] （意图识别用）
  ↓
input_processor → 安全检查 + 文件处理
  ↓
intent_recognition（用虚拟文件的 summary 组装历史）
  ↓
reasoning 循环（SystemPrompt + conversation_history + 当前 messages）
  ↓
子 Agent 执行工具 → 结果处理:
  ├→ 前端: CustomContent 按 UI 组件 schema 渲染（不进入 LLM 上下文）
  ├→ 当前轮: >500字符 → LLM 摘要 → ToolMessage 回传 reasoning
  └→ 虚拟文件: FileInfo(content=原文, summary=摘要) 供当前会话引用
  ↓
answer_question → 生成最终回复
  → >500字符 → LLM 生成 answerSummary → 写入 DB
  ↓
下次对话 → 从 DB 加载 answerSummary 构建 conversation_history
```
  ↓
意图识别（用虚拟文件中的 conversation_history 摘要）
  ↓
Reasoning 循环（SystemPrompt + history + messages）
  ↓
子 Agent 执行工具 → 结果处理:
  ├→ 前端: CustomContent 按 UI 组件 schema 渲染（不进入 LLM 上下文）
  ├→ 当前轮: >500字符 → LLM 摘要 → ToolMessage 回传 reasoning
  └→ 虚拟文件: FileInfo(content=原文, summary=摘要) 供当前会话引用
  ↓
最终回复 → >500字符 → LLM 生成 answerSummary → 写入 DB
  ↓
下次对话 → 从 DB 加载 answerSummary（而非完整 answer）
```

---

## 五、设计哲学对比

### 5.1 核心设计理念

| 理念 | Claude Code | Hermes Agent | neo-apps |
|------|------------|--------------|----------|
| 压缩范式 | **事后压缩**：完整消息链膨胀后逐级压缩 | **事后压缩**：完整消息链膨胀后四阶段压缩 | **源头隔离**：工具结果不进入历史上下文，存储时就生成摘要 |
| 工具结果的归宿 | 留在消息链中，逐步清理 | 留在消息链中，逐步清理 | 走前端 UI 组件渲染，LLM 上下文只存摘要 |
| 对 prompt cache 的态度 | 极致尊重 | 有意识优化 | 不涉及 |
| 对信息丢失的态度 | 接受有损，通过恢复机制补偿 | 接受有损，通过迭代更新减缓衰减 | 工具原文存虚拟文件（当前会话可引用），历史只存摘要 |
| LLM 摘要成本 | Session Memory 分摊；Full Compact 共享缓存 | 每次压缩调用辅助 LLM | 每次工具结果 >500 字符时调用；每次回复 >500 字符时调用 |
| 复杂度 | 极高（5 层级联 + 恢复 + 熔断） | 中等（双层 + 可插拔） | 低（源头隔离 + 按需摘要） |

### 5.2 三种范式的本质区别

```
Claude Code / Hermes 的思路:
  所有消息（user + assistant + tool_call + tool_result）→ 完整进入上下文
  → 上下文膨胀 → 触发压缩 → 裁剪/摘要/清理

neo-apps 的思路:
  工具结果 → 前端 UI 组件渲染（不进入历史上下文）
  工具结果 → 当前轮次 LLM 摘要 → ToolMessage 回传
  最终回复 → answerSummary 写入 DB
  历史加载 → 只取 query + answerSummary
  → 上下文从源头就是"瘦"的，不需要事后压缩
```

---

## 六、关键差异深度分析

### 6.1 Claude Code 独有：Session Memory（零成本摘要）

后台 Fork Agent 持续维护结构化笔记（11 section），压缩时直接用笔记作为摘要，零额外 LLM 调用。本质是**增量备份**。

### 6.2 Claude Code 独有：Cached Microcompact（缓存编辑）

利用 Anthropic API `cache_edits` 在服务端缓存中删除旧工具结果，本地消息不修改，prompt cache 不受影响。Anthropic 平台独有能力。

### 6.3 Hermes 独有：可插拔上下文引擎 + 迭代摘要 + 信息摘要替换

Hermes 的精华不仅在架构层面（可插拔引擎），更在工程细节：

1. **信息摘要替换**（vs 通用占位符）：`_summarize_tool_result()` 为 20+ 种工具生成专用的一行摘要，保留最有价值的信息（命令、退出码、文件路径、匹配数）。这比 Claude Code 的 `cache_edits`（直接删除）和通用 `[Old tool result cleared]` 都保留了更多信息。

2. **三遍裁剪**：去重 → 信息摘要 → 参数截断，每一遍解决不同的膨胀源。特别是 Pass 1 的去重（同一文件读 5 次只保留最新）和 Pass 3 的参数截断（write_file 的 50KB content 参数）是其他系统没有的。

3. **最后用户消息锚定**（#10896 修复）：确保最近的 user 消息始终在 tail 保护区中。这解决了一个微妙的 bug——边界对齐可能把最后的 user 消息推入 middle 区被摘要，导致 LLM 丢失当前任务。

4. **Resolved Questions section**：摘要模板中专门有一个 section 记录"已回答的问题及答案"，防止压缩后 LLM 重复回答已解决的问题。这是其他系统都没有的设计。

5. **反抖动保护**：连续 2 次压缩节省 <10% 时自动跳过，防止无限循环。Claude Code 用熔断器（3 次失败停止），但 Hermes 的方案更精细——它检测的是"压缩无效"而非"压缩失败"。

### 6.4 neo-apps 独有：源头隔离 + UI 组件分流 + answerSummary 回写

这是三个系统中**唯一不依赖"事后压缩"的方案**：

1. **UI 组件分流**：工具结果通过 `CustomContent`（BANT 卡片、客户画像、音频文件卡片等）推送给前端渲染，组件数据不进入 LLM 上下文。这是 2B 产品的天然优势——前端有标准组件体系，工具结果的"展示"和"理解"可以分离。

2. **工具结果摘要**：`process_sub_agent_result` 对 >500 字符的结果调用 LLM 生成摘要，摘要（而非原文）作为 ToolMessage 回传 reasoning 循环。原文存入 `FileInfo` 虚拟文件供当前会话内引用。

3. **answerSummary 回写**：`answer_question_node` 在生成最终回复后，对 >500 字符的回复调用 LLM 生成摘要，写入 `message_.answerSummary`。下次加载历史时，`conversation_history` 使用 answerSummary，虚拟文件中 content 保留完整 answer、summary 保留 answerSummary。

4. **双套历史的精妙设计**：新版 Agent 同时维护两套历史视图——`conversation_history`（answerSummary，用于 reasoning 循环，保持上下文精简）和虚拟文件 `file_list`（完整 answer + answerSummary，用于意图识别和需要引用原文时）。这比单纯的"只存摘要"或"只存原文"更灵活。

**这种方案的优势**：上下文从源头就是"瘦"的，不需要复杂的事后压缩机制。在 CRM 场景中，工具返回的是结构化业务数据（客户列表、商机统计、BANT 分析），这些数据的"展示"由前端组件负责，LLM 只需要理解摘要即可做出下一步决策。

**这种方案的劣势**：LLM 在 reasoning 循环中看不到工具结果的完整细节，可能影响多步推理的准确性。如果用户追问"刚才查到的第 3 个客户的详细信息"，LLM 无法从历史上下文中找到（因为只有摘要）。

---

## 七、适用场景与局限性

| 场景 | 最佳方案 | 原因 |
|------|---------|------|
| 编码助手（频繁读文件、运行命令） | Claude Code | 5 层级联 + prompt cache 极致优化 + 压缩后文件恢复 |
| 通用长对话 Agent | Hermes | 可插拔引擎 + 迭代摘要 + 多 LLM 后端支持 |
| 2B 业务 Agent（有前端组件体系） | neo-apps | 源头隔离天然适合：工具结果走组件渲染，LLM 上下文保持精简 |
| 需要工具结果完整保留 | Claude Code > Hermes > neo-apps | Claude Code 磁盘持久化；Hermes 在压缩前完整保留；neo-apps 当前轮有虚拟文件但历史丢失 |
| 需要最低实现复杂度 | neo-apps > Hermes > Claude Code | neo-apps 源头隔离最简单；Hermes 中等；Claude Code 极复杂 |

---

## 八、对 neo-apps 的改进建议

neo-apps 的"源头隔离"方案在 CRM 场景下是合理的设计选择，但仍有改进空间：

### 8.1 虚拟文件跨轮次持久化

**优先级：高**

当前 `FileInfo` 虚拟文件只在当前会话的 LangGraph State 中存在，会话结束即消失。如果将虚拟文件持久化（如存入 Redis 或数据库），用户追问历史工具结果时可以回读。

### 8.2 answerSummary 质量优化

**优先级：高**

当前 `ANSWER_QUESTION_SUMMARY_PROMPT` 和 `SUMMARY_PROMPT` 是通用模板。可以按业务场景（客户分析、Pipeline 分析、Coaching）使用不同的摘要模板，保留更多业务关键信息（精确数字、客户名称、商机阶段等）。

### 8.3 工具结果摘要的阈值优化

**优先级：中**

当前固定 500 字符阈值。可以按工具类型动态调整——查询类工具（返回列表数据）可以用更低阈值触发摘要，分析类工具（返回洞察结论）可以用更高阈值保留更多原文。

### 8.4 借鉴 Hermes 的迭代摘要

**优先级：中**

当前每次 `answerSummary` 都是独立生成的。如果在生成摘要时传入上一轮的摘要，要求"更新而非重新摘要"，可以在多轮对话中保持更好的信息连贯性。

### 8.5 借鉴 Claude Code 的 Session Memory

**优先级：低（但价值高）**

在 Agent 执行间隙，后台维护一份结构化的会话笔记。这份笔记可以替代当前的 `conversation_history` 虚拟文件，提供更高质量的历史上下文。

---

## 九、总结

三个系统代表了上下文管理的**三种根本不同的范式**：

1. **Claude Code — 事后压缩的工程极致**：完整消息链 → 五层级联压缩。适合编码场景（工具结果就是代码/日志，LLM 必须看到完整内容才能推理）。

2. **Hermes Agent — 事后压缩的架构优雅**：完整消息链 → 可插拔四阶段压缩。适合通用场景，扩展性最强。

3. **neo-apps — 源头隔离的业务驱动**：工具结果走前端组件渲染 + LLM 上下文只存 query + answerSummary。适合 2B 业务场景（有标准 UI 组件体系，工具结果的"展示"和"理解"可以分离）。

**核心洞察**：neo-apps 的方案不是"压缩做得不够好"，而是**根本不需要 Claude Code / Hermes 那种事后压缩**——因为它从架构层面就把工具结果隔离在了 LLM 上下文之外。这是 2B 产品（有前端组件体系）相对于 CLI 工具（Claude Code）和通用 Agent 运行时（Hermes）的天然优势。

但这种方案的代价是：LLM 在多步推理中只能看到工具结果的摘要，无法引用完整细节。如果未来需要支持更复杂的多步推理（如"基于刚才查到的客户数据做交叉分析"），可能需要引入类似 Claude Code 的虚拟文件磁盘持久化 + 按需回读机制。

---

## 二、apps-agent 记忆对比分析

# apps-agent 记忆设计 vs 当前 VikingMemoryEngine 对比分析

> 基于 wiki pageId=154702382（记忆场景测试数据）和 pageId=152089637（会话管理&记忆管理）

---

## 一、测试数据分析

wiki 页面最后的测试结果展示了 apps-agent 对一系列对话的提取效果：

### 1.1 测试结果原始数据

```
输入对话（按顺序）:
  P1: "我是张磊磊，30岁，在上海做软件开发"
  P2: "我换到上海了"（之前说在北京）
  PR2: "我写代码喜欢用 Python，注释用中文"
  PR3: "其实我更喜欢表格形式"（之前是 bullet point）
  PR4: "回答简短一点，用中文，代码用 TypeScript"
  AR1: "你以后不要主动推荐产品，只回答我问的问题"
  AR2: "另外回答要简洁，不超过100字"
  E1: "我的客户叫李四，是某科技公司的 CTO"
  E2: "合同编号 CONTRACT-001，金额50万，签约方是ABC公司"
  E4: "我们公司在杭州滨江区，主要做 SaaS 业务"
  E5: "王五是我们的销售总监，负责华东区，主要客户是XYZ集团"
  + 额外: "我是CRM销售" "销售易CEO为史总"

最终存储结果:
  entities L0: "张磊磊，30岁，上海的软件开发人员，CRM销售，销售易CEO为史总，
               公司位于杭州滨江区，主营SaaS业务。"
  entities L1: 结构化 Markdown（个人信息 + 用户身份 + 实体信息 + 公司信息）
  entities L2: 4 条独立记忆
    - "用户的身份是CRM销售人员。"
    - "销售易是一家企业，史总是该企业的CEO。"
    - "用户表示其公司位于杭州滨江区，主要从事SaaS业务。"
    - "张磊磊是一名30岁的软件开发人员，现居住在上海。"

  preference L0: "用户偏好简洁、100字以内的回答，代码默认用 TypeScript，注释用中文。"
  preference L1: 聚合描述
  preference L2: 3 条独立记忆
    - "用户在编写代码注释时习惯使用中文。"
    - "用户倾向于代码示例默认采用 TypeScript。"
    - "用户习惯于收到简洁明了的回答，并且要求每次回复内容不超过100字。"

  agent_rules L2: "回答需简洁，不超过100字"
  profile L2: "所属行业: CRM、软件开发、SaaS；职位: 销售、软件开发；姓名: 张磊磊"
```

### 1.2 关键观察

| 观察点 | apps-agent 行为 | 说明 |
|:---|:---|:---|
| L0 是聚合摘要 | entities L0 把所有实体信息合并成一句话 | L0 不是单条记忆的摘要，是整个类别的聚合 |
| L1 是结构化目录 | entities L1 用 Markdown 标题组织所有实体 | L1 是导航层，不是单条记忆的概览 |
| L2 是独立条目 | 每条 L2 是一个独立的事实 | L2 才是真正的记忆单元 |
| preference 合并了 | "表格形式"覆盖了"bullet point" | 同 aspect 的偏好被正确合并 |
| preference 拆分了 | 3 条独立的 L2（注释语言、代码语言、回复风格） | 不同 aspect 独立存储 |
| profile 是蒸馏的 | 从多条对话中提炼出行业/职位/姓名 | 不是原文，是结构化提炼 |
| agent_rules 独立类别 | "不推荐产品"+"简洁100字" | 我们没有这个类别，归入 preferences |

### 1.3 L0/L1/L2 的层级关系（apps-agent 的理解）

```
apps-agent 的三层是 目录级别 的，不是 单条记忆级别 的：

  L0（目录摘要）: 整个 entities 类别的一句话聚合
    "张磊磊，30岁，上海的软件开发人员，CRM销售..."

  L1（目录结构）: 整个 entities 类别的结构化 Markdown
    "## 个人信息\n- 姓名：张磊磊\n## 实体信息\n- 企业：销售易..."

  L2（叶子节点）: 每条独立的记忆
    "用户的身份是CRM销售人员。"
    "销售易是一家企业，史总是该企业的CEO。"
    ...

我们当前的三层是 单条记忆级别 的：

  每条记忆都有自己的 L0/L1/L2:
    L0: "华为科技/ERP项目: 张总倾向方案B"
    L1: "## 洞察\n- 决策人倾向方案B\n## 建议\n- 先展示ROI"
    L2: "华为科技ERP项目关键决策人张总倾向方案B..."
```

---

## 二、apps-agent 提取提示词分析

### 2.1 与我们的关键差异

| 维度 | apps-agent | 我们的 VikingMemoryEngine |
|:---|:---|:---|
| 提取原则 | "个性化信息 + 长期有效 + 具体清晰" | "只提取人说的，不提取工具返回的" |
| 系统数据过滤 | 无（会提取金额、阶段等系统数据） | 有（排除 CRM 系统字段） |
| agent_rules 类别 | 独立类别 | 无（归入 preferences） |
| 偏好粒度 | 详细的粒度规则（每个 aspect 独立） | 有但不如 apps-agent 详细 |
| 混淆澄清 | 有（"计划做X"→events，"项目X状态"→entities） | 无 |
| 工具/技能提取 | 从 [ToolCall] 记录提取，含 tool_name/skill_name | 纯 Python 统计，不调 LLM |
| 删除边界 | 详细的"非破坏性优先"规则 | 简单的 skip/create/merge/delete |
| 输出语言 | 支持 output_language 参数 | 固定中文 |

### 2.2 apps-agent 提示词的优势

**1. 偏好粒度规则更详细**

```
apps-agent 明确要求:
  - 每个记忆项代表一个"可独立更新的偏好单元"
  - 不要将不相关的偏好方面混合在一个记忆项中
  - 方面是开放且语义化的，非固定分类法
  - 给了正反示例（❌ 混合 vs ✅ 拆分）

我们的提示词:
  - 只说了"preferences 按方面拆分"
  - 没有正反示例
```

**2. 混淆澄清规则**

```
apps-agent 明确列出常见混淆:
  - "计划做X" → events（行动，非实体）
  - "项目X状态：Y" → entities（描述实体）
  - "用户偏好X" → preferences（非profile）
  - "遇到问题A，使用解决方案B" → cases（非events）
  - "处理某类问题的一般流程" → patterns（非cases）

我们的提示词:
  - 没有混淆澄清
  - 测试中确实出现了分类偏差（场景3: entities→preferences）
```

**3. 去重的"非破坏性优先"原则**

```
apps-agent 的去重规则:
  - 部分冲突用 merge，不要 delete
  - 仅在整个已有记忆完全失效时才 delete
  - 不确定时选择非破坏性行为（skip 或 merge）
  - 绝不能因为主题/方面不匹配而删除

我们的 DEDUP_PROMPT:
  - 有"部分冲突用merge不要delete"
  - 但没有"绝不能因主题不匹配而删除"这条关键规则
```

### 2.3 apps-agent 提示词的不足（相对于我们的 CRM 场景）

```
1. 没有系统数据过滤
   apps-agent 会提取"合同编号 CONTRACT-001，金额50万"这类系统数据
   在 CRM 场景中这些数据会过时

2. 没有"只提取人说的"原则
   apps-agent 的提示词没有区分工具返回的数据和人说的话
   在有 ToolMessage 的场景中可能提取系统数据快照

3. agent_rules 类别在 CRM 场景中价值不大
   CRM Agent 的行为规则由系统提示词控制
   用户说"不要推荐产品"更适合归入 preferences
```

---

## 三、记忆合并逻辑对比

### 3.1 apps-agent 的合并流程

```
候选记忆
  │
  ▼
向量预筛: 搜索已有相似记忆
  │
  ├── 无相似记忆 → 直接 create
  │
  └── 有相似记忆 → LLM 精判
        │
        ├── skip: 候选冗余，不存储，不修改已有
        ├── create: 候选是新信息，独立存储
        │     可选: 同时 delete 完全失效的已有记忆
        ├── none + merge: 候选不存储，但合并到已有记忆
        └── none + delete: 候选不存储，但删除已有记忆

关键规则:
  - 候选级别决策（skip/create/none）和已有记忆逐项操作（merge/delete）是分开的
  - 一个候选可以触发多个已有记忆的操作
  - merge 时生成新的 abstract + overview + content（三层都更新）
  - delete 前有严格的边界检查（部分冲突不删除）
```

### 3.2 我们的合并流程

```
候选记忆
  │
  ▼
按类别路由:
  ├── profile → 始终追加合并（content 拼接）
  ├── preferences/entities/patterns → 按 merge_key 替换
  └── events/cases → 语义去重（向量预筛 + LLM 精判）

LLM 精判（仅 events/cases）:
  ├── skip: 重复
  ├── create: 新信息
  ├── merge: 合并（删旧写新）
  └── delete: 使旧记忆失效
```

### 3.3 关键差异

| 维度 | apps-agent | 我们 |
|:---|:---|:---|
| 合并粒度 | 候选级别 + 已有记忆逐项操作 | 候选级别单一决策 |
| merge 内容 | LLM 生成合并后的 L0+L1+L2 | 直接替换（entities）或拼接（profile） |
| 多目标操作 | 一个候选可触发多个已有记忆的 merge/delete | 一个候选只影响一个已有记忆 |
| 删除边界 | 严格的非破坏性规则 | 简单的 skip/create/merge/delete |
| profile 合并 | 单文件累积（始终合并） | 同（content 追加） |
| preferences 合并 | 向量预筛 + LLM 决策 + MERGE | 按 merge_key 直接替换 |

---

## 四、需要对齐的改进项

### 4.1 高优先级（影响提取质量）

```
1. 偏好粒度规则
   在 EXTRACTION_PROMPT 中增加:
   - "每个 preferences 记忆代表一个可独立更新的偏好单元"
   - "不要将不相关的偏好方面混合在一个记忆中"
   - 正反示例（❌ 混合 vs ✅ 拆分）

2. 混淆澄清规则
   在 EXTRACTION_PROMPT 中增加:
   - "计划做X" → events
   - "项目X状态" → entities
   - "用户偏好X" → preferences（非 profile）
   - "问题A + 解决方案B" → cases（非 events）
   - "通用流程" → patterns（非 cases）

3. 去重的非破坏性原则
   在 DEDUP_PROMPT 中增加:
   - "绝不能因为主题/方面不匹配而删除"
   - "部分冲突时保留无冲突的信息"
   - "不确定时选择 skip 或 merge，而非 delete"
```

### 4.2 中优先级（影响合并质量）

```
4. entities 合并改为 LLM 生成
   当前: 同 merge_key 直接替换
   改进: 向量预筛 + LLM 决策（和 apps-agent 一致）
   效果: 合并时保留旧记忆中仍有效的信息

5. preferences 合并加 LLM 判断
   当前: 按 merge_key 直接替换
   改进: 向量预筛 + LLM 判断是否同一 aspect
   效果: 避免不同 aspect 的偏好被错误覆盖
```

### 4.3 低优先级（可后续补充）

```
6. tools/skills 提取加 LLM
   当前: 纯 Python 统计
   apps-agent: 从 [ToolCall] 记录提取使用洞察
   效果: 记录"web_search 用特定多词查询效果最佳"这类经验

7. agent_rules 类别
   apps-agent 有独立的 agent_rules 类别
   我们归入 preferences，功能上等价
   可后续评估是否需要独立
```


---

## 三、Hermes-Agent 对比分析

# Hermes Agent vs VikingMemoryEngine — 记忆系统对比分析

> Hermes Agent: Nous Research 开源 AI Agent 框架（103K+ Stars），2026 年 2 月发布。
> VikingMemoryEngine: 我们基于 OpenViking 范式的 CRM 长期记忆引擎。

---

## 一、架构总览对比

| 维度 | Hermes Agent | VikingMemoryEngine |
|------|-------------|-------------------|
| **定位** | 通用个人 AI Agent（开发者工具） | CRM 企业级 Agent 记忆引擎 |
| **记忆层级** | 3 层：内置记忆 + 会话搜索 + 外部 Provider | 3 层：L0 摘要 + L1 概览 + L2 完整内容 |
| **存储** | MEMORY.md + USER.md（文件）+ SQLite FTS5 | 腾讯向量库 + PG + SQLite FTS5 降级 |
| **容量** | 有硬上限（MEMORY 2200 字符，USER 1375 字符） | 无硬上限（向量库 + PG 无限扩展） |
| **分类体系** | 2 类（memory + user） | 9 类（soul/profile/preferences/entities/events/cases/patterns/tools/skills） |
| **检索方式** | FTS5 全文搜索 + LLM 摘要 | BM25 混合检索 + 向量语义 + 递归目录检索 |
| **提取方式** | Agent 自主决定（通过 memory 工具） | LLM 结构化提取（EXTRACTION_PROMPT） |
| **用户建模** | 外部 Provider（Honcho 辩证建模） | 内置 SOUL（用户对话定义 Agent 角色） |
| **技能系统** | 内置（从经验中自动生成 Markdown 技能文件） | 内置（skills 类记忆 + SkillRegistry） |
| **遗忘机制** | 容量满时 Agent 自行整理（无自动遗忘） | TTL + 热度自动遗忘（过期且低热度 → 删除） |

---

## 二、记忆存储对比

### Hermes Agent — 文件 + 容量上限

```
~/.hermes/memories/
├── MEMORY.md    ← Agent 个人笔记（2200 字符上限，~800 tokens）
├── USER.md      ← 用户画像（1375 字符上限，~500 tokens）
└── state.db     ← 会话历史（SQLite FTS5，无限制）

特点：
- 每次会话开始时，MEMORY.md + USER.md 作为冻结快照注入 system prompt
- 会话中修改立即写入磁盘，但不更新当前会话的 system prompt（保护 prefix cache）
- 容量满时 Agent 需要自行整理（合并/删除旧条目腾出空间）
- 总 token 开销固定 ~1300 tokens
```

### VikingMemoryEngine — 向量库 + PG + 无限扩展

```
腾讯向量库（运行态）:
├── entities     ← 客户/商机/联系人洞察（语义检索）
├── events       ← 决策/里程碑/计划（语义检索）
├── cases        ← 问题+解决方案（语义检索）
└── patterns     ← 经验归纳的规律（语义检索）

PG 数据库（查询展示）:
├── profile      ← 用户身份（精确查询）
├── preferences  ← 用户偏好（精确查询）
├── soul         ← Agent 角色定义（精确查询）
├── tools        ← 工具使用统计（精确查询）
└── skills       ← 技能执行经验（精确查询）

特点：
- 每次会话开始时，SOUL 注入 system prompt + 检索相关记忆注入 memory_context
- 无容量上限，按类别分存储路由
- 自动语义去重 + LLM 合并
- 检索时动态注入（不是冻结快照）
```

### 关键差异

| 维度 | Hermes | VikingMemoryEngine |
|------|--------|-------------------|
| 容量 | 硬上限 ~1300 tokens | 无上限 |
| 注入方式 | 冻结快照（会话开始时一次性注入） | 动态注入（每轮检索相关记忆） |
| 管理方式 | Agent 自主管理（add/replace/remove） | 自动提取 + 自动去重合并 |
| 容量满处理 | Agent 自行整理腾空间 | 自动遗忘（TTL + 热度） |
| 一致性 | 会话内不变（prefix cache 友好） | 每轮可能不同（检索结果随查询变化） |

---

## 三、记忆提取对比

### Hermes — Agent 自主决定

```
Hermes 的记忆提取是"Agent 自己决定要记什么"：

1. Agent 在对话中发现值得记住的信息
2. Agent 主动调用 memory(action="add", target="memory/user", content="...")
3. 写入 MEMORY.md 或 USER.md
4. 如果容量满，Agent 需要先 replace/remove 旧条目

优点：Agent 有完全的自主权，记什么不记什么由 Agent 判断
缺点：依赖 Agent 的"记忆意识"，可能遗漏重要信息
```

Hermes 还有一个"nudge"机制——定期提醒 Agent 检查是否有值得记住的信息：

```
系统会在对话中插入 nudge 提示：
"Review the conversation so far. Is there anything worth saving to memory?"

Agent 收到 nudge 后决定是否调用 memory 工具。
```

### VikingMemoryEngine — LLM 自动提取

```
我们的记忆提取是"LLM 自动从对话中提取"：

1. 每轮对话结束后，自动调用 _llm_extract()
2. EXTRACTION_PROMPT 指导 LLM 按 9 类分类提取
3. 语义去重 + 分类合并策略自动处理
4. 写入向量库 + PG

优点：不依赖 Agent 的"记忆意识"，不会遗漏
缺点：LLM 提取可能有误差（分类错误、重复提取）
```

### 关键差异

| 维度 | Hermes | VikingMemoryEngine |
|------|--------|-------------------|
| 提取触发 | Agent 主动调用工具 + 定期 nudge | 每轮对话后自动提取 |
| 分类粒度 | 2 类（memory / user） | 9 类（细粒度分类） |
| 去重 | 精确重复检测 | 语义去重 + LLM 精判 |
| 合并 | Agent 手动 replace | 自动按 merge_key 合并 |
| 可控性 | 高（Agent 完全控制） | 中（LLM 自动，人工可干预） |
| 覆盖率 | 依赖 Agent 判断 | 更全面（自动提取不遗漏） |

---

## 四、用户建模对比

### Hermes — USER.md + Honcho 辩证建模

```
内置层：
  USER.md（1375 字符）— 用户偏好、沟通风格、技术水平
  Agent 自主维护，手动 add/replace/remove

外部层（Honcho Provider）：
  辩证用户建模（Dialectic User Modeling）
  - 不是简单的 key-value 存储
  - 通过多轮"辩证推理"构建用户模型
  - AI Peer 和 User Peer 互相观察
  - 自动生成 representation（用户画像）和 peer card（交互风格）
  - 支持多 profile（coder/writer 等不同角色的独立建模）
```

### VikingMemoryEngine — SOUL + Profile + Preferences

```
三层用户建模：
  soul         — 用户对 Agent 的角色定义（"你是我的数据分析助理"）
  profile      — 用户自己的身份信息（"我是华东区销售总监"）
  preferences  — 用户的偏好和习惯（"我喜欢表格展示"）

SOUL 合并机制：
  - 每次提取到 soul 类记忆时，LLM 合并所有 soul 条目
  - 生成一段连贯的 SOUL 文本，注入 system prompt
  - 矛盾时以新定义为准（"之前让你用表格，现在改图表"）
```

### 关键差异

| 维度 | Hermes (USER.md + Honcho) | VikingMemoryEngine (SOUL) |
|------|--------------------------|--------------------------|
| 建模方式 | 辩证推理（多轮 LLM 对话） | 对话提取 + LLM 合并 |
| 用户定义 Agent | SOUL.md 文件（手动编写） | 对话中定义（自动提取） |
| 多角色支持 | 支持（多 profile，每个独立建模） | 支持（每个 user_id 独立 SOUL） |
| 边界区分 | 不区分（都在 USER.md） | 严格区分 soul/profile/preferences |
| 更新方式 | Agent 手动 replace | 自动合并（矛盾以新为准） |
| 复杂度 | 高（Honcho 辩证推理链路长） | 中（提取 + 合并两步） |

---

## 五、记忆检索对比

### Hermes — FTS5 + LLM 摘要

```
内置检索：
  session_search 工具 → SQLite FTS5 全文搜索 → LLM 摘要
  适合"上周我们讨论了什么"这类回忆性查询

外部检索（取决于 Provider）：
  OpenViking: 分层检索（L0→L1→L2）+ 目录浏览
  Honcho: 语义搜索 + 辩证推理
  Mem0: 语义搜索 + 重排序
  Hindsight: 知识图谱 + 多策略检索
  Holographic: FTS5 + HRR 代数查询
```

### VikingMemoryEngine — BM25 混合 + 递归目录

```
检索流程：
  1. 查询改写（LLM 解析代词）
  2. 意图分析（生成 1-3 个类型化子查询）
  3. BM25 混合检索（关键词 0.7 + 向量 0.3）
  4. 递归目录展开（α=0.5 分数传播，3 轮收敛）
  5. 热度更新（active_count += 1）
```

### 关键差异

| 维度 | Hermes | VikingMemoryEngine |
|------|--------|-------------------|
| 默认检索 | FTS5 关键词搜索 | BM25 + 向量混合检索 |
| 语义理解 | 依赖外部 Provider | 内置（向量语义 + 意图分析） |
| 层级检索 | 外部 Provider（OpenViking） | 内置（递归目录 + 分数传播） |
| 检索时机 | Agent 主动调用 session_search | 每轮自动检索注入 |
| 热度统计 | 无 | active_count（影响遗忘决策） |

---

## 六、记忆遗忘对比

### Hermes — 容量驱动，Agent 手动整理

```
遗忘策略：
  - 没有自动遗忘机制
  - 容量满时（MEMORY 2200 字符 / USER 1375 字符），Agent 需要自行整理
  - Agent 通过 replace（合并）或 remove（删除）腾出空间
  - 本质上是"容量压力驱动的被动遗忘"

优点：Agent 有完全控制权，不会误删重要记忆
缺点：依赖 Agent 的整理能力，可能保留过时信息
```

### VikingMemoryEngine — TTL + 热度自动遗忘

```
遗忘策略：
  - 按类别设置保留天数（events 90天，entities 180天，patterns 365天）
  - profile/preferences/soul 永不遗忘
  - 删除条件：过期 AND 低热度（active_count < 3）
  - 过期但高热度 → 保留（经常被检索到说明仍有价值）
  - 触发方式：手动调用 cleanup_expired() 或 reflect_global() 自动触发

优点：自动化，不需要 Agent 干预
缺点：可能误删仍有价值但未被检索到的记忆
```

### 关键差异

| 维度 | Hermes | VikingMemoryEngine |
|------|--------|-------------------|
| 触发方式 | 容量满时 Agent 手动 | 定时自动 + 手动 |
| 遗忘依据 | Agent 判断 | TTL + 热度 |
| 永不遗忘 | 无（所有记忆都可能被替换） | profile/preferences/soul |
| 热度感知 | 无 | 有（active_count） |
| 安全性 | 高（Agent 控制） | 中（自动化有误删风险） |

---

## 七、技能系统对比

### Hermes — 自动生成 Markdown 技能文件

```
闭环学习循环：
  1. Agent 完成复杂任务
  2. 自动抽象为可复用的 Markdown 技能文件
  3. 技能文件包含：执行步骤、决策点、失败模式、验证逻辑
  4. 下次遇到类似任务时，检索并复用技能
  5. 技能在使用中自我改进

存储：~/.hermes/skills/ 目录下的 .md 文件
标准：兼容 agentskills.io 开放标准
社区：Skills Hub 共享技能
```

### VikingMemoryEngine — skills/patterns 类记忆

```
技能记忆：
  - skills 类：技能执行经验（"Pipeline报告: 阶段统计→负责人统计→环比"）
  - patterns 类：从经验归纳的可重复规律
  - tools 类：工具使用统计（纯 Python 统计，不调 LLM）

存储：PG（skills/tools）+ 向量库（patterns）
合并：同技能名/工具名合并
```

### 关键差异

| 维度 | Hermes | VikingMemoryEngine |
|------|--------|-------------------|
| 技能生成 | 自动从任务中抽象 | LLM 提取 + 手动注册 |
| 技能格式 | Markdown 文件（结构化） | 记忆条目（abstract/content） |
| 自我改进 | 有（使用中自动优化） | 有（同 merge_key 合并更新） |
| 社区共享 | 有（Skills Hub + agentskills.io） | 无 |
| 工具统计 | 无独立统计 | 有（tools 类，纯 Python 统计） |

---

## 八、外部 Provider 生态对比

Hermes 的一大优势是 **8 个外部记忆 Provider 插件**，可以按需选择：

| Provider | 类型 | 特色 | 对标我们的能力 |
|----------|------|------|--------------|
| **Honcho** | 云端 | 辩证用户建模，多 Peer 观察 | SOUL 用户建模 |
| **OpenViking** | 自托管 | 文件系统层级，分层检索 | VikingFS + 递归目录检索 |
| **Mem0** | 云端 | 服务端 LLM 提取，自动去重 | _llm_extract + _dedup_and_store |
| **Hindsight** | 云端/本地 | 知识图谱，实体关系，reflect 合成 | reflect_on_session + entities |
| **Holographic** | 本地 | HRR 代数查询，信任评分 | 无对标（我们没有信任评分） |
| **RetainDB** | 云端 | 混合搜索，7 种记忆类型，delta 压缩 | BM25 混合检索 + 9 类分类 |
| **ByteRover** | 本地/云端 | 层级知识树，压缩前提取 | VikingFS 目录树 |
| **Supermemory** | 云端 | 语义搜索，会话图谱，上下文围栏 | 语义检索 + 会话反思 |

**我们的 VikingMemoryEngine 相当于 Hermes 的内置记忆 + OpenViking Provider + Mem0 Provider 的组合能力。**

---

## 九、优劣势总结

### Hermes 的优势

1. **插件化架构** — 8 个 Provider 可选，按需组合，不锁定
2. **Agent 自主管理** — 记忆的增删改由 Agent 自己决定，可控性高
3. **容量有界** — 固定 ~1300 tokens，system prompt 开销可预测
4. **技能自动生成** — 闭环学习循环，技能在使用中自我改进
5. **社区生态** — Skills Hub、agentskills.io 开放标准、103K+ Stars
6. **冻结快照** — 会话内记忆不变，prefix cache 友好，推理性能好

### Hermes 的劣势

1. **容量硬上限** — MEMORY 2200 字符 + USER 1375 字符，CRM 场景不够用
2. **无自动提取** — 依赖 Agent 的"记忆意识"，可能遗漏重要信息
3. **无细粒度分类** — 只有 memory/user 两类，无法区分 entities/events/cases 等
4. **无语义检索** — 内置只有 FTS5 关键词搜索，语义理解依赖外部 Provider
5. **无自动遗忘** — 容量满时才整理，可能长期保留过时信息
6. **单用户设计** — 个人 Agent，不支持多租户多用户

### VikingMemoryEngine 的优势

1. **9 类细粒度分类** — soul/profile/preferences/entities/events/cases/patterns/tools/skills
2. **自动提取** — 每轮对话后 LLM 自动提取，不遗漏
3. **BM25 混合检索** — 关键词 + 向量语义，CRM 场景客户名精确匹配
4. **递归目录检索** — 对齐 OpenViking 的 HierarchicalRetriever
5. **自动遗忘** — TTL + 热度，无需人工干预
6. **多租户多用户** — 企业级，每个 user_id 独立记忆空间
7. **SOUL 对话定义** — 用户通过对话定义 Agent 角色，不需要手写文件
8. **无容量上限** — 向量库 + PG 无限扩展

### VikingMemoryEngine 的劣势

1. **无插件化** — 记忆引擎是内置的，不支持切换 Provider
2. **LLM 提取误差** — 分类错误、重复提取、空提取（优化后准确率 ~96%）
3. **无技能自动生成** — skills 类记忆需要从对话中提取，不能从任务执行中自动抽象
4. **无社区生态** — 内部系统，无开放标准
5. **动态注入开销** — 每轮检索注入，token 开销不固定
6. **依赖外部服务** — 腾讯向量库 + PG，部署复杂度高于 Hermes 的纯文件方案

---

## 十、可借鉴的设计

### 从 Hermes 可借鉴

| 设计 | 说明 | 落地建议 |
|------|------|---------|
| **容量有界 + 冻结快照** | 固定 token 开销，prefix cache 友好 | SOUL 注入可以采用冻结快照模式 |
| **Agent 自主记忆管理** | 给 Agent 一个 memory 工具，让它自己决定记什么 | 可以作为自动提取的补充 |
| **Nudge 机制** | 定期提醒 Agent 检查是否有值得记住的信息 | 可以在会话结束时触发 |
| **安全扫描** | 记忆写入前扫描注入/泄露模式 | 应该加到 _dedup_and_store 中 |
| **技能自动生成** | 从任务执行中自动抽象可复用技能 | skills 类记忆可以增强为自动生成 |
| **子串匹配更新** | replace 用子串匹配而非精确匹配 | merge_key 匹配可以更灵活 |


---

## 四、V2-DeepAgent 差异分析与待办

# neo_agent_v2 vs DeepAgent — 代码级差异分析（最新）

> 基于 neo_agent_v2 最新代码与 DeepAgent 当前代码的逐模块对比

---

## 一、模块对照表

| neo_agent_v2 模块 | DeepAgent 对应 | 状态 | 差异说明 |
|---|---|---|---|
| `agents/lead_agent/agent.py` — `create_lead_agent()` | `langchain_agent.py` — `create_deep_agent()` | ✅ 已对齐 | 都调 `create_agent(model, tools, system_prompt, middleware, checkpointer)` |
| `agents/lead_agent/prompt.py` — `build_system_prompt()` | `prompt_builder.py` — `build_system_prompt()` | ✅ 已对齐 | DeepAgent 版本结构一致，支持技能段落 + 记忆上下文 |
| `agents/factory.py` — `make_lead_agent_with_model()` | `langchain_agent.py` — `create_deep_agent()` | ✅ 已对齐 | v2 的 factory.py 是旧入口，agent_factory.py 是新入口 |
| `agents/agent_factory.py` — `AgentFactory` | `agent_factory.py` — `AgentFactory` | ✅ 已对齐 | 都有 LRU 缓存 + max_depth 限制 |
| `agents/agent_config.py` — `AgentConfig` | 无独立文件 | ⚠️ 差异 | DeepAgent 用 `LangChainAgentConfig` dataclass，缺 YAML 定义支持 |
| `agents/agent_loader.py` — `AgentLoader` | 无 | ❌ 缺失 | 从 definitions/ 目录自动发现 agent.yaml |
| `agents/agent_registry.py` — `AgentRegistry` | 无 | ❌ 缺失 | 全局 Agent 配置注册表 |
| `agents/features.py` — `Features` | 无 | ❌ 缺失 | 特性开关（sandbox/memory/subagent/guardrail/mcp） |
| `agents/thread_state.py` — `ThreadState` | `state.py` — `GraphState` | ⚠️ 差异 | v2 用 LangGraph `MessagesState` + Artifact/ImageData；DeepAgent 用自定义 dataclass |
| `agents/streaming.py` — `SSEEvent` | `streaming.py` — `SSEEvent` | ✅ 已对齐 | 代码几乎一致 |
| `agents/checkpointer/provider.py` | `checkpointer.py` | ✅ 已对齐 | 都支持 SQLite，v2 额外有 Redis async |
| **中间件（14 vs 13）** | | | |
| `middlewares/agent_logging.py` | `middleware/agent_logging.py` | ✅ 已对齐 | v2 更详细（emoji 清理、GBK 兼容），DeepAgent 精简版 |
| `middlewares/clarification.py` | `middleware/clarification.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/dangling_tool_call.py` | `middleware/dangling_tool_call.py` | ✅ 已对齐 | 代码一致 |
| `middlewares/guardrail.py` | `middleware/guardrail.py` | ✅ 已对齐 | 代码一致 |
| `middlewares/input_transform.py` | `middleware/input_transform.py` | ✅ 已对齐 | 骨架一致 |
| `middlewares/loop_detection.py` | `middleware/loop_detection.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/memory.py` | `middleware/memory.py` | ✅ 已对齐 | 都有 MemoryEngine 协议 + NoopEngine |
| `middlewares/output_render.py` | 无 | ❌ 缺失 | UI 组件渲染钩子（Table/Report/Dashboard） |
| `middlewares/output_validation.py` | `middleware/output_validation.py` | ✅ 已对齐 | v2 更复杂（实体提取 + Skill 引用检查），DeepAgent 精简版 |
| `middlewares/reflection.py` | 无 | ⏭️ 空文件 | v2 也是空文件，暂不需要 |
| `middlewares/subagent_limit.py` | `middleware/subagent_limit.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/summarization.py` | `middleware/summarization.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/title.py` | `middleware/title.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/todo.py` | `middleware/todo.py` | ✅ 已对齐 | 逻辑一致 |
| `middlewares/tool_error_handling.py` | `middleware/tool_error_handling.py` | ✅ 已对齐 | 代码一致 |
| **工具系统** | | | |
| `tools/skills_tool.py` — Pydantic `BaseTool` | `skills_tool.py` — Pydantic `BaseTool` | ✅ 已对齐 | 都用 `args_schema = SkillsToolInput` |
| `tools/agent_tool.py` — Pydantic `BaseTool` | `agent_tool.py` — Pydantic `BaseTool` | ✅ 已对齐 | 都用 `args_schema = AgentToolInput` |
| `tools/loader.py` — `ToolLoader` | 无独立文件 | ❌ 缺失 | 按名注册 + load_tools_by_names + 自动发现 |
| **技能系统** | | | |
| `skills/types.py` — `Skill` dataclass | `skills.py` — `SkillDefinition` dataclass | ✅ 已对齐 | 字段一致 |
| `skills/parser.py` — `SkillParser` | `skills.py` — `SkillLoader.parse()` | ✅ 已对齐 | v2 拆分更细，功能等价 |
| `skills/loader.py` — `SkillLoader` | `skills.py` — `SkillLoader` | ✅ 已对齐 | 功能等价 |
| `skills/registry.py` — `SkillRegistry` | `skills.py` — `SkillRegistry` | ✅ 已对齐 | v2 多了 `__len__`/`__contains__` |
| `skills/executor.py` — `SkillExecutor` | `skills.py` — `SkillExecutor` | ⚠️ 差异 | v2 的 fork 用 `AgentFactory.build()`；DeepAgent 仍用 `create_deep_agent()` |
| `skills/validation.py` | `skills.py` — `SkillLoader.validate()` | ✅ 已对齐 | v2 拆分独立文件 |
| `skills/installer.py` | 无 | ⏭️ 占位 | v2 也是 `raise NotImplementedError` |
| **子 Agent 系统** | | | |
| `subagents/config.py` — `SubagentConfig` | `subagent_config.py` — `SubagentConfig` | ⚠️ 差异 | v2 多了 `inherit_middleware`/`middleware_config` 字段 |
| `subagents/factory.py` — `SubagentFactory` | 无 | ❌ 缺失 | 独立的子 Agent 工厂（与 AgentFactory 分离） |
| `subagents/registry.py` — `SubagentRegistry` | `subagent_config.py` — `SubagentRegistry` | ⚠️ 差异 | v2 多了 `register_config()`/`get_config()` |
| `subagents/executor.py` — `SubagentExecutor` | 无 | ❌ 缺失 | 双线程池（IO/CPU）异步执行 |
| `subagents/builtins/` | 无 | ❌ 缺失 | bash_agent + general_purpose（桩实现） |
| **记忆系统** | | | |
| `memory/storage.py` — `MemoryStorage` | 无 | ❌ 缺失 | 原子文件 I/O（写临时文件 → os.rename） |
| `memory/embedding.py` — `EmbeddingClient` | 无 | ❌ 缺失 | text-embedding-3-small 向量化 |
| `memory/vector_store.py` — `ChromaVectorStore` | 无 | ❌ 缺失 | ChromaDB 向量检索 |
| `memory/prompt.py` — `MemoryChunk` + `build_memory_prompt()` | 无 | ❌ 缺失 | 短期 + 长期记忆提示词构建 |
| `memory/queue.py` — `DebounceQueue` | 无 | ❌ 缺失 | 防抖合并记忆更新请求 |
| `memory/updater.py` — `MemoryUpdater` | 无 | ❌ 缺失 | LLM 提取关键信息更新记忆 |
| **配置系统** | | | |
| `config/loader.py` — `ConfigLoader` | 无 | ❌ 缺失 | YAML/JSON + 环境变量覆盖 |
| `config/models.py` — `AppConfig` Pydantic | 无 | ❌ 缺失 | 类型化配置模型 |
| **上传管理** | | | |
| `uploads/manager.py` — `UploadManager` | 无 | ❌ 缺失 | 文件存储 + 文档转 Markdown |
| **异常体系** | | | |
| `exceptions.py` — 8 个异常类 | `exceptions.py` — 6 个异常类 | ⚠️ 差异 | DeepAgent 缺 `SandboxError`/`MCPConnectionError`/`VectorStoreError` |
| **适配器** | | | |
| `adapter.py` — `NeoAgentV2Adapter` | `adapter.py` — `NeoAgentV2Adapter` | ⚠️ 差异 | v2 多了 `execute_agui()` AG-UI 管道 |

---

## 二、剩余差距汇总

### 已完成对齐（上一轮实现）
- ✅ 13 个中间件（vs v2 的 14 个，仅缺 OutputRender 骨架）
- ✅ SkillsTool / AgentTool — Pydantic BaseTool
- ✅ AgentFactory — LRU 缓存 + 深度限制
- ✅ 统一异常体系
- ✅ PromptBuilder 结构化提示词

### 仍需实现

| 优先级 | 模块 | 说明 | 工作量 |
|--------|------|------|--------|
| P1 | `SkillExecutor.fork` 改用 `AgentFactory` | 当前仍每次 `create_deep_agent`，无缓存 | 1h |
| P1 | `ToolLoader` | 按名注册 + `load_tools_by_names` + 目录自动发现 | 1h |
| P1 | `SubagentConfig` 补齐字段 | 加 `inherit_middleware`/`middleware_config`/`middleware_names` | 0.5h |
| P1 | `Features` 特性开关 | 控制中间件加载 | 0.5h |
| P2 | `AgentLoader` + `AgentRegistry` | YAML 定义自动发现 | 2h |
| P2 | `OutputRenderMiddleware` | UI 组件渲染骨架 | 0.5h |
| P2 | `memory/` 完整体系 | Storage + Embedding + VectorStore + Queue + Updater + Prompt | 4h |
| P2 | `config/` 配置体系 | ConfigLoader + AppConfig Pydantic | 2h |
| P2 | `uploads/` 上传管理 | UploadManager + 文档转 Markdown | 1h |
| P2 | `ThreadState` 替换 `GraphState` | MessagesState + Artifact + ImageData | 2h |
| P2 | `SubagentFactory` + `SubagentExecutor` | 独立子 Agent 工厂 + 双线程池 | 2h |
| P2 | `adapter.py` 补 AG-UI | `execute_agui()` 方法 | 1h |
| P3 | 异常补齐 | SandboxError/MCPConnectionError/VectorStoreError | 0.5h |

### DeepAgent 独有（需保留）
- `plugin.py` — Plugin 生命周期（v2 无此体系）
- `crm_backend.py` + `crm_tools.py` + `crm_skills.py` — CRM 业务模拟
- `tools.py` — Tool 高级特性（aliases/search_hint/should_defer/max_result_size_chars）
- `llm_client.py` — MockLLMClient 可编程测试
- `service_backend.py` — 微服务调用抽象
- `async_agent.py` — fire-and-forget 异步任务
