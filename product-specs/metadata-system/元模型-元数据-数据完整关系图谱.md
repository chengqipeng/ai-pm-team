# 元模型 → 元数据 → 数据 完整关系图谱

> 版本：1.0 | 日期：2026-04-22

---

## 一、59 个元模型完整清单

### 定义层（metamodel_layer=1）— 4 个

| apiKey | 标签 | 父元模型 | 子元模型 | entity_dependency |
|---|---|---|---|---|
| **metaModel** | 元模型定义 | — | metaItem, metaLink, metaOption | 0 |
| **metaItem** | 元模型字段 | metaModel | metaOption | 0 |
| **metaLink** | 元模型关联 | metaModel | — | 0 |
| **metaOption** | 元模型选项 | metaModel, metaItem | — | 0 |

### 实例层 — type=1 实体元数据（22 个）

| apiKey | 标签 | 父元模型 | 子元模型 | entity_dependency | 独立/子 |
|---|---|---|---|---|---|
| **entity** | 业务对象 | — | item, entityLink, checkRule, busiType, duplicateRule, fieldSet, sharingRule, dataPermission | 0 | **独立** |
| **item** | 字段 | entity | pickOption, referenceFilter, formulaCompute, aggregationCompute, itemDependency | 1 | 子 |
| **entityLink** | 关联关系 | entity | — | 1 | 子 |
| **checkRule** | 校验规则 | entity | — | 1 | 子 |
| **pickOption** | 选项值 | item | — | 1 | 子 |
| **referenceFilter** | 关联过滤 | item | referenceFilterDetail | 1 | 子 |
| **referenceFilterDetail** | 关联过滤明细 | referenceFilter | — | 1 | 子 |
| **busiType** | 业务类型 | entity | busiTypePickOption, busiTypeMapping | 1 | 子 |
| **busiTypePickOption** | 业务类型选项 | busiType | — | 1 | 子 |
| **busiTypeMapping** | 业务类型映射 | busiType | busiTypeMappingDetail | 1 | 子 |
| **busiTypeMappingDetail** | 映射明细 | busiTypeMapping | — | 1 | 子 |
| **duplicateRule** | 查重规则 | entity | duplicateRuleCriteria, duplicateMatchingRule | 1 | 子 |
| **duplicateRuleCriteria** | 查重条件 | duplicateRule | — | 1 | 子 |
| **duplicateMatchingRule** | 查重匹配 | duplicateRule | — | 1 | 子 |
| **formulaCompute** | 计算公式 | item | formulaComputeItem, computeFactor | 1 | 子 |
| **formulaComputeItem** | 公式引用字段 | formulaCompute | — | 1 | 子 |
| **aggregationCompute** | 汇总累计 | item | aggregationComputeDetail, computeFactor | 1 | 子 |
| **aggregationComputeDetail** | 汇总条件 | aggregationCompute | — | 1 | 子 |
| **computeFactor** | 计算因子 | formulaCompute, aggregationCompute | — | 1 | 子 |
| **fieldSet** | 字段集 | entity | fieldSetItem | 1 | 子 |
| **fieldSetItem** | 字段集字段 | fieldSet | — | 1 | 子 |
| **entitySvg** | 实体图标 | — | — | 0 | **独立** |

### 实例层 — type=2 组织权限（6 个）

| apiKey | 标签 | 父元模型 | 子元模型 | entity_dependency | 独立/子 |
|---|---|---|---|---|---|
| **role** | 角色 | — | — | 0 | **独立** |
| **department** | 部门 | — | — | 0 | **独立** |
| **sharingRule** | 共享规则 | entity | sharingRuleCondition | 1 | 子 |
| **sharingRuleCondition** | 共享规则条件 | sharingRule | — | 1 | 子 |
| **dataPermission** | 数据权限 | entity | — | 1 | 子 |
| **publicGroupMember** | 公共组成员 | — | — | 0 | **独立** |

### 实例层 — type=3 UI 页面（4 个）

| apiKey | 标签 | 父元模型 | 子元模型 | entity_dependency | 独立/子 | enable_ui_config |
|---|---|---|---|---|---|---|
| **menu** | 菜单 | — | — | 0 | **独立** | 0 |
| **page** | 页面 | — | pageSection | 0 | **独立** | 0 |
| **pageSection** | 页面区域 | page | pageAction | 0 | 子 | 0 |
| **pageAction** | 页面动作 | pageSection | — | 0 | 子 | 0 |

### 实例层 — type=4 UI 组件（5 个）

| apiKey | 标签 | enable_ui_config |
|---|---|---|
| **uiComponent** | UI组件 | 0 |
| **uiComposite** | UI复合组件 | 0 |
| **uiButtonStyle** | 按钮样式 | 0 |
| **uiAction** | 事件动作 | 0 |
| **uiIcon** | 图标 | 0 |

### 实例层 — type=5 系统运维（6 个）

| apiKey | 标签 | enable_ui_config |
|---|---|---|
| **metaOperateLog** | 元数据操作日志 | 0 |
| **entityOperateLog** | 业务数据操作日志 | 0 |
| **metaLog** | 元数据变更日志 | 0 |
| **excelTask** | 导入导出任务 | 0 |
| **fileMeta** | 文件元信息 | 0 |
| **metadataRef** | 元数据引用注册 | 0 |

### 实例层 — type=6 认证安全（7 个）

| apiKey | 标签 |
|---|---|
| **totpSecret** | TOTP密钥 |
| **passwordPolicy** | 密码策略 |
| **authProvider** | 认证提供商 |
| **oauthClient** | OAuth客户端 |
| **loginPolicy** | 登录策略 |
| **ipWhitelist** | IP白名单 |
| **thirdBinding** | 第三方绑定 |

### 实例层 — type=7 数据基础设施（4 个）

| apiKey | 标签 | enable_ui_config |
|---|---|---|
| **tenantDataRoute** | 分表路由 | 0 |
| **tenantDataStats** | 分表统计 | 0 |
| **dataShare** | 权限共享记录 | 0 |
| **dataShareRoute** | Share分表路由 | 0 |

### 实例层 — type=8 业务扩展（6 个）

| apiKey | 标签 |
|---|---|
| **position** | 岗位 |
| **positionUser** | 岗位用户关联 |
| **assistantRelation** | 助理关系 |
| **teamMember** | 团队成员 |
| **territory** | 销售区域 |
| **territoryMember** | 区域成员 |

### 实例层 — type=9 全局配置（5 个）

| apiKey | 标签 | 父元模型 | 子元模型 | 独立/子 |
|---|---|---|---|---|
| **globalPickOption** | 全局选项集 | — | globalPickDependency | **独立** |
| **globalPickDependency** | 选项集依赖 | globalPickOption | globalPickDependencyDetail | 子 |
| **globalPickDependencyDetail** | 依赖明细 | globalPickDependency | — | 子 |
| **itemDependency** | 字段依赖 | item | itemDependencyDetail | 子 |
| **itemDependencyDetail** | 依赖明细 | itemDependency | — | 子 |


## 二、p_meta_link 完整关系（34 条）

### 定义层内部（4 条）

```
metaModel ──→ metaItem       (metamodelApiKey)
metaModel ──→ metaLink       (parentMetamodelApiKey)
metaModel ──→ metaOption     (metamodelApiKey)
metaItem  ──→ metaOption     (itemApiKey)
```

### entity 直接子元模型（8 条）

```
entity ──→ item              (entityApiKey)
entity ──→ entityLink        (entityApiKey)
entity ──→ checkRule         (entityApiKey)
entity ──→ busiType          (entityApiKey)
entity ──→ duplicateRule     (entityApiKey)
entity ──→ fieldSet          (entityApiKey)
entity ──→ sharingRule       (entityApiKey)
entity ──→ dataPermission    (entityApiKey)
```

### item 直接子元模型（5 条）

```
item ──→ pickOption          (itemApiKey)
item ──→ referenceFilter     (itemApiKey)
item ──→ formulaCompute      (itemApiKey)
item ──→ aggregationCompute  (itemApiKey)
item ──→ itemDependency      (controlItemApiKey)
```

### 深层子元模型（11 条）

```
referenceFilter     ──→ referenceFilterDetail     (filterApiKey)
busiType            ──→ busiTypePickOption         (busiTypeApiKey)
busiType            ──→ busiTypeMapping            (busiTypeApiKey)
busiTypeMapping     ──→ busiTypeMappingDetail      (mappingApiKey)
duplicateRule       ──→ duplicateRuleCriteria       (ruleApiKey)
duplicateRule       ──→ duplicateMatchingRule       (ruleApiKey)
formulaCompute      ──→ formulaComputeItem          (computeApiKey)
formulaCompute      ──→ computeFactor               (computeApiKey)
aggregationCompute  ──→ aggregationComputeDetail    (aggregateApiKey)
aggregationCompute  ──→ computeFactor               (computeApiKey)
itemDependency      ──→ itemDependencyDetail        (dependencyApiKey)
```

### globalPickOption 体系（2 条）

```
globalPickOption       ──→ globalPickDependency        (entityApiKey)
globalPickDependency   ──→ globalPickDependencyDetail   (dependencyApiKey)
```

### page 体系（需补充，当前缺失）

```
page        ──→ pageSection     (entityApiKey)     ← 需补充到 p_meta_link
pageSection ──→ pageAction      (entityApiKey)     ← 需补充到 p_meta_link
```

### fieldSet 体系（1 条）

```
fieldSet ──→ fieldSetItem    (fieldSetApiKey)
```

### sharingRule 体系（1 条）

```
sharingRule ──→ sharingRuleCondition   (ruleApiKey)
```

## 三、entityLink 实例级关联（元数据之间）

### entity 体系（从远程数据库同步，30+ 条）

```
account     ──→ contact          "客户关联联系人"
account     ──→ opportunity      "客户关联商机"
account     ──→ lead             "客户关联线索"
opportunity ──→ contact          "商机关联联系人"
...（每对 entity 之间的关联关系）
```

### 独立元模型（4 条种子数据）

```
role        ──→ role             "角色自关联（上级角色）"     linkType=0
role        ──→ user             "角色关联用户"              linkType=2
department  ──→ department       "部门自关联（上级部门）"     linkType=0
department  ──→ user             "部门关联用户"              linkType=2
```

## 四、完整层级树（所有父子关系）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 定义层（metamodel_layer=1）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
metaModel
├── metaItem
│   └── metaOption
├── metaLink
└── metaOption

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — entity 体系（type=1，最深 5 层）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
entity                                          ← 独立，enable_data_source=1
├── item                                        ← entity_dependency=1
│   ├── pickOption
│   ├── referenceFilter
│   │   └── referenceFilterDetail
│   ├── formulaCompute
│   │   ├── formulaComputeItem
│   │   └── computeFactor
│   ├── aggregationCompute
│   │   ├── aggregationComputeDetail
│   │   └── computeFactor                       ← 共享（两个父）
│   └── itemDependency
│       └── itemDependencyDetail
├── entityLink
├── checkRule
├── busiType
│   ├── busiTypePickOption
│   ├── busiTypeMapping
│   │   └── busiTypeMappingDetail
├── duplicateRule
│   ├── duplicateRuleCriteria
│   └── duplicateMatchingRule
├── fieldSet
│   └── fieldSetItem
├── sharingRule
│   └── sharingRuleCondition
└── dataPermission

entitySvg                                       ← 独立，entity_dependency=0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — 组织权限（type=2）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
role                                            ← 独立
  entityLink: role→role（自关联）, role→user
department                                      ← 独立
  entityLink: department→department（自关联）, department→user
publicGroupMember                               ← 独立

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — UI 页面（type=3，enable_ui_config=0）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
menu                                            ← 独立
page                                            ← 独立
└── pageSection
    └── pageAction

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — UI 组件（type=4，enable_ui_config=0）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
uiComponent                                     ← 独立
uiComposite                                     ← 独立
uiButtonStyle                                   ← 独立
uiAction                                        ← 独立
uiIcon                                          ← 独立

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — 全局配置（type=9）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
globalPickOption                                ← 独立
└── globalPickDependency
    └── globalPickDependencyDetail

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 实例层 — 认证安全（type=6）、数据基础设施（type=7）、业务扩展（type=8）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
全部独立，无父子关系：
totpSecret, passwordPolicy, authProvider, oauthClient, loginPolicy,
ipWhitelist, thirdBinding, position, positionUser, assistantRelation,
tenantDataRoute, tenantDataStats, dataShare, dataShareRoute,
teamMember, territory, territoryMember

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 系统运维（type=5，enable_ui_config=0）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
全部独立：metaOperateLog, entityOperateLog, metaLog, excelTask, fileMeta, metadataRef
```

## 五、跨层关系

### 定义层 → 实例层（隐含，通过 metamodel_api_key 关联）

```
metaModel 记录 'entity'      →  entity 的所有实例（account, opportunity, lead, ...）
metaModel 记录 'item'        →  item 的所有实例（3000+ 条字段定义）
metaModel 记录 'role'        →  role 的所有实例（systemAdmin, salesManager, ...）
metaModel 记录 'department'  →  department 的所有实例（companyRoot, techDept, ...）
metaModel 记录 'page'        →  page 的所有实例（pageUserList, pageDeptTree, ...）
...（每个 metaModel 记录对应一组元数据实例）
```

### 实例层 → 数据层（通过 enable_data_source + p_tenant_data_route）

```
entity 实例 'account'        →  p_tenant_data_132（客户业务数据）
entity 实例 'opportunity'    →  p_tenant_data_456（商机业务数据）
entity 实例 'lead'           →  p_tenant_data_789（线索业务数据）
entity 实例 'user'           →  p_user（用户数据，独立表）
```

## 六、独立元模型汇总（画布"元数据"分类应显示的）

以下元模型是**顶层独立的**（无父元模型，entity_dependency=0），应该直接出现在画布数据源"元数据"分类中：

| apiKey | 标签 | type | 有子元模型 | 有 entityLink |
|---|---|---|---|---|
| **entity** | 业务对象 | 1 | ✅ 8 个直接子 | ✅ account→contact 等 |
| **entitySvg** | 实体图标 | 1 | — | — |
| **role** | 角色 | 2 | — | ✅ role→user |
| **department** | 部门 | 2 | — | ✅ department→user |
| **publicGroupMember** | 公共组成员 | 2 | — | — |
| **globalPickOption** | 全局选项集 | 9 | ✅ 2 个子 | — |
| **position** | 岗位 | 8 | — | — |
| **teamMember** | 团队成员 | 8 | — | — |
| **territory** | 销售区域 | 8 | — | — |
| **totpSecret** | TOTP密钥 | 6 | — | — |
| **passwordPolicy** | 密码策略 | 6 | — | — |
| **authProvider** | 认证提供商 | 6 | — | — |
| **oauthClient** | OAuth客户端 | 6 | — | — |
| **loginPolicy** | 登录策略 | 6 | — | — |
| **ipWhitelist** | IP白名单 | 6 | — | — |
| **thirdBinding** | 第三方绑定 | 6 | — | — |

以下元模型是**子元模型**（有父元模型，entity_dependency=1 或有 p_meta_link 父记录），**不应**直接出现在画布"元数据"分类顶层，只在左侧选了父元模型后通过联动约束显示：

item, entityLink, checkRule, pickOption, referenceFilter, referenceFilterDetail,
busiType, busiTypePickOption, busiTypeMapping, busiTypeMappingDetail,
duplicateRule, duplicateRuleCriteria, duplicateMatchingRule,
formulaCompute, formulaComputeItem, aggregationCompute, aggregationComputeDetail,
computeFactor, fieldSet, fieldSetItem, itemDependency, itemDependencyDetail,
sharingRule, sharingRuleCondition, dataPermission,
globalPickDependency, globalPickDependencyDetail,
pageSection, pageAction,
positionUser, assistantRelation, territoryMember

## 七、缺失的 p_meta_link 记录（需补充）

| apiKey | 父 → 子 | referItemApiKey | 说明 |
|---|---|---|---|
| page_to_pageSection | page → pageSection | entityApiKey | 页面包含区域 |
| pageSection_to_pageAction | pageSection → pageAction | entityApiKey | 区域包含动作 |
| position_to_positionUser | position → positionUser | positionApiKey | 岗位包含用户 |
| territory_to_territoryMember | territory → territoryMember | territoryApiKey | 区域包含成员 |

## 八、不在元模型体系中的业务数据表

以下表通过 `@IgnoreTenantLine` 注解声明，不注册为元模型：

| 表 | Java Entity | 说明 |
|---|---|---|
| p_passport | Passport | 全局身份凭证，无 tenant_id |
| p_passport_log | PassportLog | 登录日志，无 tenant_id |
| p_user_role | UserRole | 用户角色关联，业务代码手动控制 tenant_id |
| p_user | PlatformUser | 用户数据，业务代码手动控制 tenant_id |
